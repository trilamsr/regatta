# W7 Wave 1 — Operator Web UI Scaffold + Approval Flow — Implementer Task Breakdown (2026-06-01)

Source-of-truth spec: `docs/engineer/specs/2026-06-01-w7-operator-web-ui-design.md` §1, §3.2–§3.8, §6, §7 Wave 7.1, §10 (substrate spec `0006_substrate.sql` already shipped).
Authority: `feedback_spec_pattern_authority` — implementer deviation from spec MUST re-spawn the design subagent.
Decision priority (`feedback_decision_priority`): UX → ease → performance → best-practices → speed → velocity; long-term > short-term. UX gate is the operator clicking a Slack link and approving without a JS framework + with zero third-party origin in CSP. Every scope-trim defends that gate.

---

## Wave overview

- **4 file-disjoint implementer tasks** — T4 (HTTP scaffold + CSP), T5 (Tailwind + htmx vendoring), T7 (auth + Principal + CSRF + Origin), T6 (approval handlers + templates). Spec §7 Wave 7.1 table is the source of truth.
- **Dispatch shape:** T4 + T5 + T7 parallel (file-disjoint, no internal dep edges); T6 sequenced after BOTH T4 and T7 merge to main. T6 imports the `csp.go` middleware (T4 owner) AND the `Principal` type + `CSRFMiddleware` + `OriginCheck` (T7 owner); dispatching T6 in parallel would force it to stub or vendor copies of both. Per autonomous-prompt PRIORITY #2 (sequence-dependent work): SEQUENCE T6 last.
- **Hard prereqs (merged to main):**
  - W7.0 T1 (#263 `feat(w7.0): T1 HTTP listener + InteractiveNotifier.CallbackRoute() concrete impl`) ✅ merged 2026-06-01. Provides `cmd/regatta/serve.go::bootListener(cfg listenerConfig) (*http.Server, error)` seam + `mux := http.NewServeMux()` extension surface.
  - W7.0 T2 (#255 `feat(w7.0): T2 dbtest.QueryCounter middleware`) ✅ merged 2026-06-01. `dbtest.NewQueryCounter(*sql.DB)` ships; W7.2 consumes (not Wave 1).
  - W7.0 T3 (#253 `refactor(approval): lift DecideTx into internal/gates/approval`) ✅ merged 2026-06-01. Exports `approval.DecideTx(ctx, db, payload, reviewerID, decision, reason, clock) (DecideTxResult, string, error)` — T6 imports.
  - Substrate W1 T-S1/T-S2/T-S3 (#224/#232/#233) ✅ merged. Migration `0006_substrate.sql` is on disk; W7.3 (cost panel, Wave 2) reads `substrate_events`. Wave 1 does NOT touch the substrate.
- **Migration number lock:** Wave 1 ships ZERO migrations. T4/T5/T6/T7 are all Go code + embedded assets + templates + Makefile targets. Substrate `0006` is the last applied number; if any wave-1 task uncovers a load-bearing need for schema work, the migration number is **0007** (pinned per `feedback_migration_number_lock`) — but the design subagent must be re-spawned BEFORE writing it (per `feedback_spec_pattern_authority`). Spec §3.2 reaffirms "zero modifications to approval state machine" — no schema work in Wave 1.
- **Concurrency cap:** peak 3 parallel implementers (T4 + T5 + T7); then T6 alone after T4 + T7 merge. Well within `feedback_dispatch_strategy` 10-lane cap.
- **Deletion default (`feedback_deletion_default`):** Wave 1 is mostly addition (a new `internal/web/` package, Tailwind + htmx vendored assets, templates). The deletion lens per task:
  - T4: pure addition (~250 LoC across scaffold files); the only shrink is `cmd/regatta/serve.go::bootListener`'s mux which T4 stops growing — every UI route lands inside `internal/web/` and W7.0's mux delegates via `mux.Handle("/", webHandler)` at the boot site. Net `serve.go` delta ≈ 0 (one new `mux.Handle` line). Subsequent waves do not grow `serve.go`.
  - T5: vendored assets are binary bytes (tailwind.min.css ≈ 15 KiB gzipped; htmx.min.js ≈ 50 KiB); Go source delta < 20 LoC for the `embed.FS` plumbing already on T4. Adopting OSS == zero npm-on-CI surface, which is a long-term shrink at the build-pipeline layer.
  - T6: largest task by LoC (handlers + 5 templates); the shrink is at the deferral layer — `/diff` overflow page exists ONLY because the 8 KiB cap exists; without the cap an unbounded diff template would have shipped (and broken on hostile inputs).
  - T7: every line of `auth.go` is the FIRST and ONLY caller of `canon.VerifyToken` from the web layer; the `Authorizer` interface from spec v1 is deleted (R4) — net pure-shrink vs the rejected v1 design. `Principal` type only (no interface).
- **Phase positioning:** Wave 1 ships the approval flow ONLY. NO `/runs/{run_id}` view (Wave 2 / T8-T10). NO `/runs/{run_id}/cost` panel (Wave 3 / T11). NO Playwright E2E (Wave 3 / T12). NO benchmarks (Wave 3 / T13). NO lint targets `lint-web-template-html` / `lint-hx-sync` / `lint-web-nplus1` (Wave 3 / T14). Wave 1 + Wave 0 (already shipped) = a B-grade deliverable per spec §7 "Wave 7.0 + W7.1 alone is a B-grade deliverable (approval flow only)".

---

## §1 File-disjoint table

| Task   | Path (exclusive write scope)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Depends-on (Wave 1 + main)             | Effort | TDD tests (count: named)                                                                                                                                                                                                                       |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **T4** | `internal/web/server.go` (NEW); `internal/web/server_test.go` (NEW); `internal/web/render.go` (NEW); `internal/web/render_test.go` (NEW); `internal/web/const.go` (NEW); `internal/web/csp.go` (NEW); `internal/web/csp_test.go` (NEW); `internal/web/templates/layout.tmpl` (NEW); `internal/web/templates/_flash.tmpl` (NEW); `internal/web/static/htmx-config.js` (NEW); `internal/web/doc.go` (NEW); `cmd/regatta/serve.go` (MODIFY — wire `web.NewHandler()` into `bootListener` mux at root via `mux.Handle("/", webHandler)`)                                                                                                                                                                                                                                                                                                                                                              | main (W7.0 #263 #255 #253)              | M      | 9 named (B 6, A 3). Spec §3.2 §3.3 §3.7 §3.8 + §6 B-rubric.                                                                                                                                                                                    |
| **T5** | `internal/web/css/input.css` (NEW); `internal/web/static/tailwind.min.css` (NEW — committed compiled output); `internal/web/static/htmx.min.js` (NEW — vendored htmx 2.x with SRI hash documented in T5 PR); `internal/web/tailwind.config.js` (NEW); `internal/web/static/.headers` (NEW — Makefile reads provenance for SRI verification); `Makefile` (MODIFY — add `build-tailwind` + `verify-vendored-assets` targets; wire `verify-vendored-assets` into `make check`); `internal/web/static/VENDORED.md` (NEW — provenance pin: htmx version + SHA-256 + Tailwind version + git ref)                                                                                                                                                                                                                                                                                                          | main                                    | S      | 5 named (B 4, A 1). Spec §3.5 + §6 B-rubric "No JS framework in go.mod" + "Tailwind/htmx vendored under internal/web/static/" lint.                                                                                                            |
| **T7** | `internal/web/auth.go` (NEW); `internal/web/auth_test.go` (NEW); `internal/web/csrf.go` (NEW); `internal/web/csrf_test.go` (NEW); `internal/web/origin.go` (NEW); `internal/web/origin_test.go` (NEW)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | main + T4 merged (csp.go must exist)    | M      | 10 named (B 6, A 4). Spec §3.6 + §6 B-rubric "HMAC token verify reuses internal/canon" + A-rubric "CSRF + Origin both wired ≥100 randomized cases".                                                                                            |
| **T6** | `internal/web/approval.go` (NEW); `internal/web/approval_test.go` (NEW); `internal/web/diff.go` (NEW — `clampDiff` 8 KiB helper, `[]DiffLine` type); `internal/web/diff_test.go` (NEW); `internal/web/templates/approval.tmpl` (NEW); `internal/web/templates/approval_decided.tmpl` (NEW); `internal/web/templates/approval_error.tmpl` (NEW); `internal/web/templates/approval_diff.tmpl` (NEW); `internal/web/templates/_diff.tmpl` (NEW)                                                                                                                                                                                                                                                                                                                                                                                                                                                       | main + T4 merged + T7 merged             | L      | 14 named (B 8, A 4, A+ 2). Spec §3.4 §3.6.1 §3.6.2 §6 B/A-rubric.                                                                                                                                                                              |

**Disjointness verification (grep at plan time):**
- Every path in the table is unique — no row shares a file with another row.
- T4 + T6 both write under `internal/web/templates/` but to DIFFERENT files: T4 owns `layout.tmpl` + `_flash.tmpl`; T6 owns `approval*.tmpl` + `_diff.tmpl`. File-name disjoint. (Go-build disjoint trivially since templates are not Go source.)
- T4 + T5 both write under `internal/web/static/` but to DIFFERENT files: T4 owns `htmx-config.js`; T5 owns `htmx.min.js` + `tailwind.min.css` + `VENDORED.md` + `.headers`. File-name disjoint.
- T4 is the ONLY task modifying `cmd/regatta/serve.go` in Wave 1 (one-line `mux.Handle("/", webHandler)` insertion).
- T5 is the ONLY task modifying `Makefile` in Wave 1.
- T4 + T7 + T6 each ship Go files inside `internal/web/`; ALL filenames are disjoint by enumeration (`server.go` / `render.go` / `const.go` / `csp.go` for T4; `auth.go` / `csrf.go` / `origin.go` for T7; `approval.go` / `diff.go` for T6).
- **Verdict: ZERO file overlap across the four tasks.** Plan-time `grep -E '^\| \*\*T[4-7]' docs/engineer/plans/2026-06-01-w7-wave1-tasks.md` + a one-pass shell extractor would confirm.

**Per `feedback_plan_subagent_dup_files`** every dispatch prompt below cites the EXACT output path slug per task; implementers cannot accidentally collide on a new file because the prompt enumerates the full exclusive write list.

---

## Cross-task seam contracts (load-bearing — implementers MUST honour exactly)

These are the exports each task surfaces. Pinning them at plan time prevents the "T6 redesigns `Principal` because T7's signature wasn't obvious" failure mode (per `feedback_shared_primitive_owner`).

### T4 exports (consumed by T6 + T7)

```go
// internal/web/server.go
package web

// NewHandler builds the top-level http.Handler that cmd/regatta wires into bootListener's mux. The returned handler is the SOLE entry point for UI routes; it owns sub-mux dispatch + CSP middleware composition.
func NewHandler(deps Dependencies) http.Handler { ... }

// Dependencies bundles the side-effect handles the web layer needs. Mirrors approval.Dependencies (W7.0 T1) shape so the seam is familiar.
type Dependencies struct {
    DB        *state.DB
    Keyring   canon.Keyring
    Templates *Templates       // from render.go
    Clock     func() time.Time
    Config    Config           // see below
}

// Config holds the boot-time knobs the handler reads (decision window for cookie TTL, CSRF cookie name, etc.) — kept narrow to delay an interface until W8.
type Config struct {
    DecisionWindow time.Duration  // mirrored from approval gate config; cookie Max-Age = this
    PublicHost     string         // future --public-url; v1 empty + falls back to r.Host
}
```

```go
// internal/web/render.go
package web

// Templates is the parsed embed.FS template set; loaded once at NewHandler and shared across handlers (parse-at-boot, fail-loud per spec §3.4).
type Templates struct{ ... }

// Render writes the named template to w with data; centralises the html/template invocation so handlers cannot bypass auto-escape.
func (t *Templates) Render(w http.ResponseWriter, name string, data any) error { ... }

// LoadTemplates parses templates/*.tmpl from the embedded fs at boot. Caller passes the package-level //go:embed FS; T6 reuses the same FS so its templates compile alongside T4's.
func LoadTemplates(fsys fs.FS) (*Templates, error) { ... }
```

```go
// internal/web/csp.go
package web

// CSPMiddleware sets the spec §3.7 headers (Content-Security-Policy, Referrer-Policy, X-Content-Type-Options, X-Frame-Options) on every response. Returns a handler that wraps next; T7 composes alongside CSRFMiddleware and OriginCheck.
func CSPMiddleware(next http.Handler) http.Handler { ... }
```

```go
// internal/web/const.go (no exported funcs; named constants only — A+ scorecard "zero magic numbers")
package web

const (
    CSRFCookieName            = "regatta_csrf"
    ApprovalTokenCookieName   = "regatta_approval_token"
    MaxDiffBytes              = 8 * 1024
    MaxFullDiffBytes          = 1 << 20  // 1 MiB hard cap on /diff overflow stream
    StaticCacheMaxAgeSeconds  = 86400
    PollIntervalSeconds       = 5
    // ... every magic number in internal/web/ MUST be named here.
)
```

### T5 exports (consumed by T4)

T5 ships ZERO Go exports — it lands binary assets + a Makefile target. T4's `//go:embed static` picks up the vendored bytes automatically once both PRs are merged.
- Asset paths: `internal/web/static/tailwind.min.css`, `internal/web/static/htmx.min.js`. T4's `layout.tmpl` references `/ui/static/tailwind.min.css` + `/ui/static/htmx.min.js`. T5 documents the URL contract in `VENDORED.md` so the asset paths are not load-bearing on file-rename whim.
- Makefile target: `make verify-vendored-assets` MUST pass on `make check`. It diffs the on-disk SHA-256 of `htmx.min.js` against the pinned hash in `VENDORED.md`; mismatch fails CI. This is the supply-chain anchor (closes spec §8 #3 even though §8 #3 says "N/A in v2" — the SRI verification at build time is the safety net IF the file is ever tampered with).

### T7 exports (consumed by T6)

```go
// internal/web/auth.go
package web

// Principal carries the W7 reviewer identity; W8 RBAC will widen Roles + populate Tenant. Per spec §3.6.4 + R4: type only, no Authorizer interface in v1.
type Principal struct {
    ID     string   // W7: payload.Reviewer; W8: session.UserID
    Tenant string   // W7: "default"; W8: session.Tenant
    Roles  []string // W7: nil; W8: session.Roles
}

// PrincipalFromRequest reads the HMAC cookie set by RedeemHandler, verifies via canon.VerifyToken, and returns the Principal. Sentinel errors map 1:1 to spec §6 typed sentinels (ErrTokenInvalid, ErrTokenExpired, ErrTokenReplay, ErrUnknownKey).
func PrincipalFromRequest(r *http.Request, kr canon.Keyring, now time.Time) (Principal, canon.TokenPayload, error) { ... }

// RedeemHandler implements the spec §3.6.1 step 1+2 redirect: GET /approve/redeem?t=<wire> → set HMAC cookie (HttpOnly, SameSite=Strict, Secure, scoped Path=/approve/{aid}) + set CSRF cookie + 303 → /approve/{aid}.
func RedeemHandler(deps Dependencies) http.Handler { ... }

// Sentinel errors per spec §3.6.1 + §6 typed sentinels.
var (
    ErrCookieMissing  = errors.New("web: approval cookie missing")
    ErrTokenInvalid   = errors.New("web: token invalid")
    ErrTokenExpired   = errors.New("web: token expired")
    ErrTokenReplay    = errors.New("web: token replay")
    ErrUnknownKey     = errors.New("web: unknown signing key")
    ErrNotReviewer    = errors.New("web: principal not in reviewer set")
    ErrSelfReview     = errors.New("web: self-review blocked by gate policy")
)
```

```go
// internal/web/csrf.go
package web

// CSRFMiddleware enforces double-submit-cookie on POST /approve/{aid}/decide. Cookie regatta_csrf (32-hex, HttpOnly, SameSite=Strict, Path=/approve/) issued on first GET after redeem; POST form must echo the same value back via hidden input. subtle.ConstantTimeCompare for the match.
func CSRFMiddleware(next http.Handler) http.Handler { ... }

// CSRFTokenFromRequest reads the cookie value; template func `csrfToken` calls this at render time to populate the hidden input.
func CSRFTokenFromRequest(r *http.Request) string { ... }
```

```go
// internal/web/origin.go
package web

// OriginCheck rejects POST requests whose Origin header is missing or does not match "https://"+r.Host. Defense-in-depth alongside CSRF cookie (spec §3.6.2 S2). htmx 2 sets Origin automatically; curl attacks without it 403.
func OriginCheck(next http.Handler) http.Handler { ... }
```

### T6 exports (consumed by T4's mux)

T6 ships HTTP handlers that T4's `NewHandler()` mounts on the sub-mux at:
- `GET  /approve/{approval_id}` → `ApprovalPageHandler`
- `POST /approve/{approval_id}/decide` → `ApprovalDecideHandler`
- `GET  /approve/{approval_id}/diff` → `ApprovalDiffOverflowHandler`

T6 exports:
```go
// internal/web/approval.go
package web

// RegisterApprovalRoutes mounts the three approval-flow handlers on the supplied mux with T7's CSRFMiddleware + OriginCheck + CSPMiddleware (from T4) chained per spec §3.6 + §3.7. T4's NewHandler calls this; keeps T4/T6 ownership clean.
func RegisterApprovalRoutes(mux *http.ServeMux, deps Dependencies) { ... }
```

```go
// internal/web/diff.go
package web

// DiffLine carries one unified-diff line; `_diff.tmpl` ranges over []DiffLine.
type DiffLine struct {
    Op           rune    // '+' | '-' | ' '
    Text         string
    OldLineNum   int
    NewLineNum   int
}

// ClampDiff truncates lines to MaxDiffBytes (8 KiB total output). Returns the kept slice + a boolean signalling overflow (template uses to show "Show full diff →" link).
func ClampDiff(lines []DiffLine, max int) (kept []DiffLine, overflowed bool) { ... }
```

### Owner declarations (per `feedback_shared_primitive_owner`)

- **T4 is OWNER of**: `internal/web/server.go::NewHandler` + `Dependencies` + `Config` types, `internal/web/render.go::Templates` + `LoadTemplates`, `internal/web/csp.go::CSPMiddleware`, `internal/web/const.go` constants. Any T6/T7 deviation from these signatures requires re-spawning T4's design subagent.
- **T5 is OWNER of**: `internal/web/static/tailwind.min.css` + `htmx.min.js` byte payload + `VENDORED.md` provenance pin. Asset URL paths (`/ui/static/...`) are documented in T5's PR body; T4 reads the contract.
- **T7 is OWNER of**: `Principal` type + `PrincipalFromRequest` + `RedeemHandler` + sentinel errors + `CSRFMiddleware` + `CSRFTokenFromRequest` + `OriginCheck`. T6 imports these by name; signature changes block T6's PR.
- **T6 is OWNER of**: `DiffLine` + `ClampDiff` + `RegisterApprovalRoutes` + the approval template set.

---

## §2 Task T4 — HTTP scaffold + CSP middleware + render + healthz extension point

### Scope (exclusive write paths)

- `internal/web/server.go` (NEW):
  - `package web`. `//go:embed templates static` declares the package-level `assetsFS`.
  - `NewHandler(deps Dependencies) http.Handler` constructs the sub-mux: `/ui/static/*` (asset server with `Cache-Control: public, max-age=86400, immutable`), `/approve/redeem` (T7 — handler reference only; T4 mounts via `RedeemHandler(deps)`), `/approve/...` (T6 — `RegisterApprovalRoutes(mux, deps)`). Wraps the whole thing in `CSPMiddleware`.
  - `Dependencies` + `Config` types per the cross-task seam contracts above.
- `internal/web/render.go` (NEW):
  - `Templates` struct + `LoadTemplates(fsys fs.FS) (*Templates, error)`. Parses ALL `templates/*.tmpl` at boot via `template.New("layout").Funcs(funcMap).ParseFS(fsys, "templates/*.tmpl")`. Parse failure → boot error (loud, per spec §3.4).
  - `funcMap` whitelist: `formatTime`, `formatBytes`, `formatTokens`, `formatUSDMicros`, `truncate`, `humanizeDuration`, `safeURL`, `csrfToken`, `clampDiff` (T6 imports the func registration but `ClampDiff` itself lives in T6's `diff.go` — T4 registers a placeholder that T6 replaces; OR cleaner: T4 ships only the funcs it needs and T6 extends via an exported `RegisterFunc(name, fn any)` on `*Templates`). Pick the latter — keeps T4/T6 decoupled.
- `internal/web/csp.go` (NEW):
  - `CSPMiddleware(next http.Handler) http.Handler` sets spec §3.7 headers byte-equal. Wire-format CSP single line. Plus `Referrer-Policy: no-referrer`, `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`.
  - Sentinel constant `cspHeader` lives here so the test asserts string equality (catches accidental drift).
- `internal/web/const.go` (NEW):
  - Every magic number the package uses, named (A+ scorecard prereq). Listed in the cross-task seam above.
- `internal/web/doc.go` (NEW):
  - ONE line package godoc: `// Package web ships the operator HTTP surface for regatta serve --ui (spec docs/engineer/specs/2026-06-01-w7-operator-web-ui-design.md).`
- `internal/web/templates/layout.tmpl` (NEW):
  - HTML doc skeleton. `<link rel="stylesheet" href="/ui/static/tailwind.min.css">` + `<script src="/ui/static/htmx.min.js" defer></script>` + `<script src="/ui/static/htmx-config.js" defer></script>`. NO inline script (CSP-strict).
- `internal/web/templates/_flash.tmpl` (NEW):
  - Green/red banner partial.
- `internal/web/static/htmx-config.js` (NEW):
  - Tiny `htmx.config.allowEval = false;` set + `htmx.config.selfRequestsOnly = true;` set. Loaded after htmx.min.js via `defer`. Zero inline script.
- `cmd/regatta/serve.go` (MODIFY):
  - Inside `bootListener`, immediately after the existing `cbPath, cbHandler := approval.NewHTTPCallback(...)` block, construct `web.NewHandler(web.Dependencies{...})` and `mux.Handle("/", webHandler)`. The order matters: more-specific routes (`/healthz`, `/api/approval/callback`) must register BEFORE the catch-all. `http.ServeMux` longest-prefix wins so `/healthz` etc. still resolve correctly; spec assert with a test.
  - ONE-line `mux.Handle("/", webHandler)` insertion + 4-line `web.Dependencies{...}` literal construction. NO other changes to `serve.go`.

### Prereqs (cite spec sections)

- Spec §3.2 — package layout: `internal/web/server.go`, `render.go`, `const.go`, `csp.go`, `doc.go`, `templates/`, `static/`.
- Spec §3.3 — route table; T4 wires the route prefix mounting only.
- Spec §3.4 — template func whitelist; T4 registers `formatTime` / `formatBytes` / `formatTokens` / `formatUSDMicros` / `truncate` / `humanizeDuration` / `safeURL` / `csrfToken`. `clampDiff` is registered via the extension hook (T6 extends).
- Spec §3.5 — vendored asset URL contract (`/ui/static/...`).
- Spec §3.7 — CSP byte-string.
- Spec §3.8 — embed strategy.
- Spec §6 B-rubric items: "All 8 routes in §3.3 implemented + httptest pass" (T4 covers the scaffold + healthz extension; T6 covers approval routes; T7 covers redeem); "CSP header present + matches §3.7 verbatim (regex test); Referrer-Policy: no-referrer present"; "No third-party origins in CSP"; "No JS framework in go.mod deps".

### Existing patterns to reuse (do NOT reinvent — `feedback_research_design_principles`)

- **HTTP server**: Go stdlib `net/http`. No chi/echo/gin (rejected — see spec §2 "Rejected").
- **Mux**: `http.ServeMux`. Sub-mux composition via wrapping middleware (`CSPMiddleware(mux)`) — no router lib.
- **Embed**: Go stdlib `embed.FS`. Spec §3.8 already specifies the directive.
- **Template engine**: `html/template` stdlib. Auto-escape closes XSS by default.
- **Static file serving**: `http.FileServer(http.FS(staticSubFS))` with a `http.StripPrefix("/ui/static/", ...)` wrapper.
- **Mux extension pattern**: `cmd/regatta/serve.go::bootListener` already calls `mux.Handle(cbPath, cbHandler)` for the approval callback. T4 adds ONE more `mux.Handle("/", webHandler)` line in the same style. NO refactor of bootListener.
- **Test helpers**: `net/http/httptest`. The existing `cmd/regatta/serve_listener_test.go` is the pattern for integration-style boot tests.

### TDD test list (with failing-output capture step)

Per `feedback_tdd_discipline`: each test ships first; implementer runs `go test ./internal/web/... -run <name> -v`, captures failing output, pastes into PR body, then implements.

**B-tier (spec §6 B-rubric):**

1. `TestNewHandler_ServesHealthzAndAssets` — `NewHandler(deps)` returns a handler that responds 200 on `/ui/static/htmx.min.js` (placeholder byte: T5 ships real file; T4 ships an empty file so the test loads); empty file is fine — the test asserts the route + Content-Type + Cache-Control header, not the body.
2. `TestNewHandler_ServesLayoutTemplateOnRoot` — request `/` → 200 with `<title>` substring from `layout.tmpl`. (Or, if `/` is reserved for T6's redirect, the test asserts 404 with a typed `Content-Type: text/html`; whichever the spec demands.)
3. `TestCSPMiddleware_SetsAllSpecHeadersByteEqual` — wrap a no-op handler; GET `/anything` → response headers `Content-Security-Policy: default-src 'self'; script-src 'self'; style-src 'self'; img-src 'self' data:; connect-src 'self'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'` byte-equal; `Referrer-Policy: no-referrer`; `X-Content-Type-Options: nosniff`; `X-Frame-Options: DENY`.
4. `TestCSPMiddleware_NoThirdPartyOrigins` — assert `! strings.Contains(cspHeader, "://")` (no URLs in CSP at all, since only `'self'` keywords).
5. `TestLoadTemplates_FailsBootOnMalformedTemplate` — write a malformed template into a test FS; `LoadTemplates` returns non-nil error (spec §3.4 fail-loud).
6. `TestServe_RootHandlerWiredIntoBootListener` — call `bootListener(listenerConfig{UI: true, Addr: ":0", DB: testDB, Keyring: testKeyring})`; assert the returned `*http.Server.Handler.ServeHTTP` resolves a `/ui/static/htmx-config.js` request to the web handler (NOT 404 from the bare W7.0 mux). This is the seam-wiring proof.

**A-tier (spec §6 A-rubric + A+ scorecard):**

7. `TestRenderTemplates_AutoEscapesUserInput` — render `layout.tmpl` with `data.UserContent = "<script>alert(1)</script>"`; output body MUST contain `&lt;script&gt;` (escaped) and MUST NOT contain literal `<script>` from the input. Pins html/template auto-escape gate.
8. `TestNewHandler_CSPHeaderOnEveryRoute` — sweep every route registered by `NewHandler` (`/ui/static/*`, `/`, etc.); each response carries the CSP header verbatim. Catches a future handler that forgets to wrap with CSPMiddleware.
9. `TestConstNoZeroValueMagic` — read `internal/web/const.go` source; grep for numeric literals OUTSIDE `const (...)` block in `*.go` files of the package; expect zero (A+ scorecard "zero magic numbers"). Implemented as a `go/parser`-based AST walker (NOT regex — handles `const (8 * 1024)` arithmetic without false positives).

### PR body skeleton

```
## Summary

T4 ships the operator-web-UI HTTP scaffold per
docs/engineer/specs/2026-06-01-w7-operator-web-ui-design.md §3.2 §3.3
§3.4 §3.7 §3.8 §7 Wave 7.1 T4.

- internal/web/server.go: NewHandler(deps Dependencies) http.Handler.
- internal/web/render.go: Templates + LoadTemplates; parse-at-boot,
  fail-loud on malformed.
- internal/web/csp.go: CSPMiddleware sets spec §3.7 headers byte-equal
  + Referrer-Policy: no-referrer + X-Content-Type-Options: nosniff +
  X-Frame-Options: DENY.
- internal/web/const.go: every magic number named (A+ scorecard prereq).
- internal/web/templates/layout.tmpl + _flash.tmpl: skeleton + partial.
- internal/web/static/htmx-config.js: allowEval=false + selfRequestsOnly=true.
- cmd/regatta/serve.go: ONE-line mux.Handle("/", web.NewHandler(...))
  wire into bootListener; specific routes (/healthz, /api/approval/callback)
  retain precedence per http.ServeMux longest-prefix-wins.

## Why

MVP-3 W7 Wave 1 T4. Establishes the package surface that T5 (vendored
assets), T6 (approval handlers + templates), and T7 (auth + CSRF +
Origin) build on. Pinning the scaffold first prevents the four
implementers from racing on Dependencies-shape or Templates-shape.

## Test plan

- [x] B-tier: TestNewHandler_ServesHealthzAndAssets,
       TestNewHandler_ServesLayoutTemplateOnRoot,
       TestCSPMiddleware_SetsAllSpecHeadersByteEqual,
       TestCSPMiddleware_NoThirdPartyOrigins,
       TestLoadTemplates_FailsBootOnMalformedTemplate,
       TestServe_RootHandlerWiredIntoBootListener.
- [x] A-tier: TestRenderTemplates_AutoEscapesUserInput,
       TestNewHandler_CSPHeaderOnEveryRoute,
       TestConstNoZeroValueMagic.
- [x] make pre-push-check clean.
- [x] doc-check diff vs origin/main: every new exported godoc ≤ 1 line.

## Failing-test output (TDD capture, before impl)

<paste from terminal — required per feedback_tdd_discipline>

## Deletion default

T4 is pure addition for the new internal/web/ package (~250 LoC across
6 .go files + 2 .tmpl files + 1 .js file). What gets smaller:
- cmd/regatta/serve.go delta: +5 LoC (web.Dependencies literal + one
  mux.Handle line). The package's _scaffold_ stays inside internal/web/
  so subsequent waves do not grow serve.go.
- The funcMap extension hook (Templates.RegisterFunc) lets T6 attach
  clampDiff WITHOUT a circular import (T6 would otherwise need T4 to
  import T6's diff.go) — saves ~30 LoC of round-trip glue that a
  monolithic Templates would have shipped.

## A+ Rubric Scorecard

<paste verbatim — required per feedback_a_plus_scorecard_required>

## Followups

- F1 — extend funcMap registry for Wave 2 (`/runs/{run_id}` handlers
  will need ranges + sort funcs); tracking issue cited once Wave 2
  begins.

```release-notes
[FEATURE] internal/web/ HTTP scaffold + CSP middleware for operator UI
```
```

### Dispatch prompt (paste-ready)

```
You are an implementer subagent working on a git worktree at
.claude/worktrees/agent-w7-wave1-t4. Branch off main:
`git checkout -b feat/w7-wave1-t4-scaffold main`.

Dispatch in PARALLEL with T5 and T7 (file-disjoint per the plan's §1
table). T6 dispatches AFTER your PR merges to main.

# Spec authority

Source-of-truth spec: docs/engineer/specs/2026-06-01-w7-operator-web-ui-design.md.
Read ALL of: §3.2 (package layout — your file scope), §3.3 (route table
— you wire the mux but only register layout/static routes; T6 + T7 ship
the approval/redeem handlers), §3.4 (template funcMap whitelist —
register only the funcs you need; expose RegisterFunc for T6 extension),
§3.7 (CSP byte-string — verbatim header), §3.8 (embed.FS strategy),
§7 Wave 7.1 T4 (your scope), §6 B-rubric + A-rubric items applicable
to scaffold.

Plan: docs/engineer/plans/2026-06-01-w7-wave1-tasks.md §2 (this section)
+ §1 (cross-task seam contracts — your exports MUST match those
signatures byte-for-byte; T6 + T7 import by name).

Per feedback_spec_pattern_authority: if you want to deviate from any
spec-mandated pattern — funcMap whitelist content, CSP header string,
Dependencies struct shape, Templates struct shape, `mux.Handle("/",
webHandler)` mounting style — STOP and re-spawn the design subagent.
Do NOT pick an alternative yourself.

# Scope (exclusive write paths — file-disjoint with T5 + T6 + T7)

EXACT output path slugs (per feedback_plan_subagent_dup_files):
- internal/web/server.go (NEW)
- internal/web/server_test.go (NEW)
- internal/web/render.go (NEW)
- internal/web/render_test.go (NEW)
- internal/web/const.go (NEW)
- internal/web/csp.go (NEW)
- internal/web/csp_test.go (NEW)
- internal/web/doc.go (NEW; one-line package godoc)
- internal/web/templates/layout.tmpl (NEW)
- internal/web/templates/_flash.tmpl (NEW)
- internal/web/static/htmx-config.js (NEW)
- cmd/regatta/serve.go (MODIFY — one mux.Handle insertion only)

You MUST NOT touch any other file. Specifically:
- Do NOT create internal/web/auth.go, csrf.go, origin.go (T7's domain).
- Do NOT create internal/web/approval.go, diff.go (T6's domain).
- Do NOT create internal/web/static/htmx.min.js or tailwind.min.css
  (T5's domain — ship an empty placeholder via .gitkeep ONLY if Go's
  embed directive errors on an empty static dir; otherwise leave for
  T5).
- Do NOT add any goose migration. Migration number, if ever needed,
  is 0007 (pinned per feedback_migration_number_lock).
- Do NOT modify internal/gates/approval/ or internal/canon/.

# Patterns to reuse (feedback_research_design_principles — proven OSS > build-from-scratch)

- HTTP server: Go stdlib net/http; http.ServeMux for routing.
- Embed: Go stdlib embed.FS. //go:embed templates static is the
  directive shape (spec §3.8).
- Templates: html/template stdlib. Auto-escape is the XSS gate.
- Static serving: http.FileServer(http.FS(subFS)) + http.StripPrefix.
- Mux extension: extend cmd/regatta/serve.go::bootListener's existing
  mux. ONE additional mux.Handle("/", webHandler) line.
- Test pattern: net/http/httptest for handler tests; existing
  cmd/regatta/serve_listener_test.go pattern for the boot-listener
  integration test.

# Workflow steps (TDD discipline — feedback_tdd_discipline)

For each named test:
  1. Write test file first.
  2. Run `go test ./internal/web/... -run <name> -v` (or `./cmd/regatta/...`
     for the boot-listener integration test).
  3. CAPTURE failing output (paste into PR body's "Failing-test output
     (TDD capture)" section). "Tests would have failed" is NOT
     acceptable.
  4. Implement the minimum needed to pass.
  5. Re-run; confirm pass.
  6. Commit (one commit per test or logical group; squash later).

# Tests to land (9 named; spec §6 B/A-rubric)

B-tier:
1. TestNewHandler_ServesHealthzAndAssets
2. TestNewHandler_ServesLayoutTemplateOnRoot
3. TestCSPMiddleware_SetsAllSpecHeadersByteEqual
4. TestCSPMiddleware_NoThirdPartyOrigins
5. TestLoadTemplates_FailsBootOnMalformedTemplate
6. TestServe_RootHandlerWiredIntoBootListener

A-tier:
7. TestRenderTemplates_AutoEscapesUserInput
8. TestNewHandler_CSPHeaderOnEveryRoute
9. TestConstNoZeroValueMagic (AST-walker, not regex)

# Workflow after green

  1. Run `make pre-push-check` — confirm clean.
  2. Run `go test ./... -race` end-to-end. Any failure outside your
     scope means you broke a transitive caller — STOP and report.
  3. doc-check diff vs origin/main:
     `git diff origin/main -- 'internal/web/**/*.go' | grep -E '^\+(?!\+)' | grep '^// '`
     — every new godoc on exported funcs MUST be ≤ 1 line (per
     feedback_comments_discipline + feedback_pr_lint_gates). Trim
     multi-line godocs to one line BEFORE pushing.
  4. Sweep superfluous comments per feedback_comments_discipline:
     WHY not WHAT.
  5. NO AI signatures — do NOT add Co-Authored-By footers (per
     feedback_no_signatures).
  6. Push branch.
  7. Open PR with:
     `gh pr create --base main --title "feat(w7.1): T4 internal/web HTTP scaffold + CSP middleware + render + const" --body-file <path>`.
     USE --body-file (per feedback_pr_lint_gates), NEVER heredoc.
  8. Paste the A+ Rubric Scorecard VERBATIM in the PR body (per
     feedback_a_plus_scorecard_required + feedback_grade_rubric).
  9. After PR opens, spawn ONE adversarial reviewer subagent (per
     feedback_adversarial_review + feedback_agent_pr_review) with
     hunt list:
     - CSP header byte-equal vs spec §3.7 (paste both strings; assert
       no whitespace/ordering drift).
     - Referrer-Policy: no-referrer present (R3 — bearer-token leak
       defense-in-depth).
     - Templates parse-at-boot fails LOUD (not silent-fallback at
       render).
     - funcMap whitelist excludes any func that returns template.HTML
       (XSS-bypass guard); RegisterFunc rejects names that already
       exist.
     - http.ServeMux longest-prefix-wins: /healthz + /api/approval/
       callback still resolve to W7.0's handlers after T4's
       mux.Handle("/", ...) lands.
     - embed.FS empty-dir handling: static/ has at least
       htmx-config.js so go:embed does not error pre-T5.
     - Zero magic numbers outside const.go (AST walker, not regex).
     - Cache-Control: public, max-age=86400, immutable on /ui/static/*
       BUT no-store everywhere else.
     - X-Frame-Options: DENY + frame-ancestors: 'none' (double-belt).
     - Reviewer must use OK:/ISSUE: per item.
 10. Apply reviewer findings; re-run make pre-push-check; re-run
     doc-check diff; force-push.
 11. Enable automerge ONLY after reviewer cleared + every Risk-tier
     finding fixed inline OR filed as tracking issue cited in PR body
     (per feedback_review_before_automerge + feedback_unaddressed_load_bearing).

# Return format

Final report MUST contain:
- PR URL.
- Pasted failing-test output for at least 5 of the 9 tests.
- Adversarial reviewer verdict (APPROVE | findings list with OK:/ISSUE:).
- One-line diff stat: files changed + LoC added + LoC removed.
- A+ Rubric Scorecard verbatim.
- Confirmation: `mux.Handle("/", web.NewHandler(...))` wires into
  bootListener AFTER /healthz + /api/approval/callback registration so
  the specific routes win.
```

---

## §3 Task T5 — Vendor Tailwind compiled output + htmx 2.x + build target

### Scope (exclusive write paths)

- `internal/web/css/input.css` (NEW):
  - 3 lines: `@tailwind base;` + `@tailwind components;` + `@tailwind utilities;`. Source for the compiled output.
- `internal/web/tailwind.config.js` (NEW):
  - `content: ['./internal/web/templates/**/*.tmpl']` — Tailwind purge content; the JIT scans templates at build time.
  - `theme: { extend: {} }` — no custom theme in v1.
- `internal/web/static/tailwind.min.css` (NEW — committed compiled output):
  - Run `make build-tailwind` on a developer machine to produce; commit the output. Spec §3.5 mandates this is committed (CI does NOT run npx).
- `internal/web/static/htmx.min.js` (NEW — vendored from htmx.org GitHub release):
  - Pinned version cited in `VENDORED.md`; SHA-256 verified by `make verify-vendored-assets`.
- `internal/web/static/VENDORED.md` (NEW):
  - Provenance pin: `htmx@2.x.y` upstream URL + SHA-256 + commit-ref + retrieval date. `tailwindcss@3.x.y` upstream URL + git ref + retrieval date. Operator can re-verify supply chain from this single file.
- `internal/web/static/.headers` (NEW):
  - Comment-style file documenting the asset URL contract (path → expected `Content-Type` + `Cache-Control`). Reference document for T4 + future reverse-proxy operators.
- `Makefile` (MODIFY):
  - Add `build-tailwind` target: `npx tailwindcss -i ./internal/web/css/input.css -o ./internal/web/static/tailwind.min.css --minify --content ./internal/web/templates/**/*.tmpl`. Documented as "developer-machine-only; commit the output". Idempotent.
  - Add `verify-vendored-assets` target: `shasum -a 256 internal/web/static/htmx.min.js | awk '{print $1}'` compared against the pinned hash extracted from `VENDORED.md`. Mismatch fails CI.
  - Wire `verify-vendored-assets` as a dependency of the existing `check` target.

### Prereqs (cite spec sections)

- Spec §3.5 — Tailwind compiled to a static dist at build time; `tailwind.min.css` committed; CI does NOT run npx.
- Spec §3.7 — CSP allows only `'self'` for `style-src` + `script-src`; vendored bytes are the only way to satisfy.
- Spec §6 B-rubric — "No JS framework in go.mod deps; htmx + Tailwind vendored under internal/web/static/ (lint)".
- Spec §8 #3 — N/A in v2 because vendored; SRI verification at the Makefile gate is the supply-chain anchor.
- Existing patterns to reuse:
  - Existing `Makefile` style — phony targets, no shell-loops; `help` target auto-discovers via `## comment` convention.
  - `shasum -a 256` (stdlib on macOS + Linux) for SRI verification — no third-party tool.

### TDD test list

T5 is asset-vendoring; the "tests" are CI gates + a shell-script smoke test packaged via Go. Per `feedback_tdd_discipline` write the test (or the Makefile target) FIRST, capture the failing state (asset missing → grep fails / shasum mismatch → make fails), then ship.

**B-tier:**

1. `TestVendoredAssets_TailwindFileExists` — Go test in `internal/web/static/static_test.go` (lives inside the static dir? No — put it at `internal/web/static_test.go` if Go disallows `_test.go` in non-package dirs; or use the `embed.FS` itself via a test in `internal/web/`). Asserts `assetsFS.Open("static/tailwind.min.css")` succeeds + the file size is between 5 KiB and 50 KiB (sanity range).
2. `TestVendoredAssets_HtmxFileExistsAndSHAMatchesPin` — opens `static/htmx.min.js` via `assetsFS`; computes SHA-256; compares against the pinned hex string in a const inside the test file (the const is sourced from `VENDORED.md` — single source of truth, but the test pins the value so a silent edit fails CI).
3. `TestVendoredAssets_NoNPMDepInGoMod` — `grep -E '(node|npm|browserify)' go.mod` returns zero matches (catches an accidental `tool` directive importing a node-bridge).
4. `TestMakeCheck_VerifyVendoredAssetsTargetWired` — runs `make -n check` (dry-run); asserts `verify-vendored-assets` appears in the recipe chain. (Or: lint by parsing the Makefile AST — pick whichever survives `make` version drift across `make-3.81`/`make-4.x`.)

**A-tier:**

5. `TestVendoredAssets_TailwindContentPurgedNotJIT` — read tailwind.min.css; assert string `@tailwind` does NOT appear (build ran successfully and purged); assert file size ≤ 50 KiB (sanity: untouched CDN bundle would be ≥150 KiB; purge cuts it).

### PR body skeleton

```
## Summary

T5 ships Tailwind (compiled output, committed) + htmx 2.x (vendored,
SRI-pinned) under internal/web/static/ per
docs/engineer/specs/2026-06-01-w7-operator-web-ui-design.md §3.5 +
§6 B-rubric "Tailwind/htmx vendored, no JS framework in go.mod".

- internal/web/static/tailwind.min.css: build-time compiled, committed.
- internal/web/static/htmx.min.js: vendored htmx 2.x, SHA-256 pinned in
  VENDORED.md.
- internal/web/static/VENDORED.md: provenance + supply-chain anchor.
- Makefile build-tailwind: developer-machine target (npx; committed
  output).
- Makefile verify-vendored-assets: shasum gate wired into make check.

## Why

MVP-3 W7 Wave 1 T5. R3 closure (no CDN, no third-party origin in CSP,
no Referer leak to third party). Adopts proven OSS (Tailwind + htmx)
without a runtime npm dependency. Supply chain anchored at build time
via SHA-256 verification — supply-chain compromise (#8) becomes a
diff-time fail rather than a runtime surprise.

## Test plan

- [x] B-tier: TestVendoredAssets_TailwindFileExists,
       TestVendoredAssets_HtmxFileExistsAndSHAMatchesPin,
       TestVendoredAssets_NoNPMDepInGoMod,
       TestMakeCheck_VerifyVendoredAssetsTargetWired.
- [x] A-tier: TestVendoredAssets_TailwindContentPurgedNotJIT.
- [x] make pre-push-check clean (verify-vendored-assets passes).
- [x] doc-check diff vs origin/main: only Makefile recipe comments
       added; no exported godocs to lint.

## Failing-test output (TDD capture, before impl)

<paste from terminal — required per feedback_tdd_discipline>

## Deletion default

T5 adds vendored bytes (~65 KiB total: tailwind.min.css ~15 KiB + htmx.min.js
~50 KiB) + ~30 LoC Makefile recipe + ~80 LoC VENDORED.md. What gets
smaller — the long-term ops burden:
- ZERO runtime npm dependency on CI (closes spec §3.5 + R3).
- ZERO third-party origin in CSP (closes spec §3.7 + R3).
- ZERO Referer leak to CDN (defense-in-depth alongside Referrer-Policy:
  no-referrer T4 ships).
- SHA-256 pin makes the supply chain auditable in O(seconds) — any
  future "did htmx ship a backdoor" question is one shasum invocation.

## A+ Rubric Scorecard

<paste verbatim — required per feedback_a_plus_scorecard_required>

## Followups

- F2 — automate Tailwind version bumps via a `make bump-tailwind` target
  that re-runs npx and re-pins the hash (deferred until first version
  bump; tracking issue when needed).

```release-notes
[FEATURE] vendor Tailwind compiled output + htmx 2.x under internal/web/static/
```
```

### Dispatch prompt (paste-ready)

```
You are an implementer subagent working on a git worktree at
.claude/worktrees/agent-w7-wave1-t5. Branch off main:
`git checkout -b feat/w7-wave1-t5-vendor-assets main`.

Dispatch in PARALLEL with T4 and T7 (file-disjoint per the plan's §1
table). T6 dispatches AFTER T4 + T7 merge to main; you do NOT block T6
directly, but T6 references your asset URLs in templates.

# Spec authority

Source-of-truth spec: docs/engineer/specs/2026-06-01-w7-operator-web-ui-design.md.
Read: §3.5 (Tailwind compiled at build time, committed, no npx on CI;
htmx vendored from htmx.org with CSP-strict config), §3.7 (CSP only
allows 'self' for style-src + script-src — vendored bytes are the
ONLY satisfier), §6 B-rubric ("No JS framework in go.mod deps; htmx +
Tailwind vendored under internal/web/static/ (lint)"), §7 Wave 7.1 T5
(your scope), §8 #3 (CDN compromise — N/A because vendored).

Plan: docs/engineer/plans/2026-06-01-w7-wave1-tasks.md §3 + §1 (your
exports = bytes at fixed URL paths).

Per feedback_spec_pattern_authority: if you want to deviate (e.g. use
a different CSS framework, ship a CDN fallback, write the Tailwind
purge config differently, skip the SHA-256 pin), STOP and re-spawn
the design subagent.

# Scope (exclusive write paths — file-disjoint with T4 + T6 + T7)

EXACT output path slugs:
- internal/web/css/input.css (NEW)
- internal/web/tailwind.config.js (NEW)
- internal/web/static/tailwind.min.css (NEW — committed compiled output)
- internal/web/static/htmx.min.js (NEW — vendored htmx 2.x)
- internal/web/static/VENDORED.md (NEW — provenance pin)
- internal/web/static/.headers (NEW — URL contract documentation)
- Makefile (MODIFY — add build-tailwind + verify-vendored-assets;
  wire verify-vendored-assets into check)
- internal/web/static_test.go (NEW — Go test for the embed.FS asset
  presence; this file is at internal/web/static_test.go, NOT inside
  the static/ subdir, so it lives in package web)

You MUST NOT touch any other file. Specifically:
- Do NOT create or modify internal/web/server.go, render.go, csp.go,
  const.go, doc.go (T4's domain).
- Do NOT create internal/web/auth.go, csrf.go, origin.go (T7).
- Do NOT create internal/web/approval.go, diff.go (T6).
- Do NOT modify cmd/regatta/serve.go.
- Do NOT add a goose migration. Migration number, if ever needed, is
  0007 (pinned).

# Patterns to reuse (feedback_research_design_principles)

- htmx 2.x upstream: https://github.com/bigskysoftware/htmx/releases
  (MIT). Pin the SPECIFIC release tag; do NOT use `main`. Download
  the minified bundle; commit + pin SHA-256.
- Tailwind v3 upstream: https://tailwindcss.com — npx tailwindcss at
  build time, committed output.
- shasum -a 256: stdlib on macOS + Linux. NO third-party SRI tool.
- Makefile style: existing targets use `## comment` for help discovery;
  follow that convention.

# Workflow steps (TDD discipline — for asset-vendoring this means
"write the test that asserts the asset exists + hash matches BEFORE
committing the asset")

For each named test:
  1. Write test first (the test asserts an asset that does not yet
     exist — failing state captured).
  2. Run `go test ./internal/web/... -run <name> -v` (or
     `make verify-vendored-assets` for the Makefile gate).
  3. CAPTURE failing output (paste into PR body).
  4. Vendor the asset (`curl` from upstream + `shasum -a 256` to get
     the hash; update VENDORED.md with the hash).
  5. Re-run; confirm pass.
  6. Commit.

# Tests to land (5 named; spec §6 B-rubric)

B-tier:
1. TestVendoredAssets_TailwindFileExists
2. TestVendoredAssets_HtmxFileExistsAndSHAMatchesPin
3. TestVendoredAssets_NoNPMDepInGoMod
4. TestMakeCheck_VerifyVendoredAssetsTargetWired

A-tier:
5. TestVendoredAssets_TailwindContentPurgedNotJIT

# Workflow after green

  1. Run `make pre-push-check` — confirm clean (verify-vendored-assets
     passes).
  2. Run `make build-tailwind` on YOUR developer machine (you need
     npx + tailwindcss available; install via
     `npm install -D tailwindcss@3` if missing). Commit the OUTPUT
     (tailwind.min.css) BUT do NOT commit node_modules/ or
     package-lock.json — add them to .gitignore if not already.
  3. doc-check diff vs origin/main — no godocs to lint, but assert
     VENDORED.md is well-formed (parseable provenance block).
  4. NO AI signatures — do NOT add Co-Authored-By footers.
  5. Push branch.
  6. Open PR with:
     `gh pr create --base main --title "feat(w7.1): T5 vendor Tailwind + htmx 2.x under internal/web/static/" --body-file <path>`.
     USE --body-file, NEVER heredoc.
  7. Paste the A+ Rubric Scorecard VERBATIM in the PR body.
  8. After PR opens, spawn ONE adversarial reviewer subagent with hunt
     list:
     - SHA-256 hash in VENDORED.md MATCHES the on-disk hash (paste
       both; assert byte-equal).
     - htmx version pinned to a release tag, NOT `main`.
     - tailwind.min.css size ≤ 50 KiB (purge ran successfully).
     - go.mod has zero npm-bridge deps.
     - VENDORED.md cites upstream URL + license + retrieval date for
       BOTH assets.
     - Makefile verify-vendored-assets wired into `check`; assert
       `make -n check 2>&1 | grep -q verify-vendored-assets`.
     - .gitignore excludes node_modules/ + package-lock.json (so a
       future developer running build-tailwind does not accidentally
       commit them).
     - CSP wire-format: vendored URLs are all same-origin (/ui/static/
       prefix). No external URLs anywhere in the static/ dir.
     - Reviewer must use OK:/ISSUE: per item.
  9. Apply findings; re-run pre-push-check; force-push.
 10. Enable automerge ONLY after reviewer cleared.

# Return format

Final report MUST contain:
- PR URL.
- Pasted failing-test output for at least 3 of the 5 tests.
- Pasted `shasum -a 256 internal/web/static/htmx.min.js` output
  showing the hash matches VENDORED.md.
- Pasted file size: `wc -c internal/web/static/tailwind.min.css`
  (expected: 5–50 KiB).
- Adversarial reviewer verdict.
- One-line diff stat.
- A+ Rubric Scorecard verbatim.
```

---

## §4 Task T7 — Auth (Principal + cookie-bound HMAC) + CSRF (double-submit) + Origin check

### Scope (exclusive write paths)

- `internal/web/auth.go` (NEW):
  - `Principal` struct (per cross-task seam).
  - `PrincipalFromRequest(r *http.Request, kr canon.Keyring, now time.Time) (Principal, canon.TokenPayload, error)` — reads `regatta_approval_token` cookie, calls `canon.VerifyToken`, returns Principal + the verified payload (T6 uses payload.AID + payload.WI). Sentinel errors map 1:1 to spec §6 typed sentinels.
  - `RedeemHandler(deps Dependencies) http.Handler` — spec §3.6.1 redeem flow: validates `?t=<wire>` via `canon.VerifyToken`, sets HMAC cookie (HttpOnly, SameSite=Strict, Secure, scoped Path=`/approve/<aid>`, Max-Age = `cfg.DecisionWindow.Seconds()`), sets CSRF cookie (32 hex bytes from crypto/rand, HttpOnly, SameSite=Strict, Secure, Path=`/approve/`), 303 redirects to `/approve/<aid>`.
  - Sentinel errors: `ErrCookieMissing`, `ErrTokenInvalid`, `ErrTokenExpired`, `ErrTokenReplay`, `ErrUnknownKey`, `ErrNotReviewer`, `ErrSelfReview`.
- `internal/web/auth_test.go` (NEW):
  - Tests for `PrincipalFromRequest` + `RedeemHandler` (happy + every sentinel).
- `internal/web/csrf.go` (NEW):
  - `CSRFMiddleware(next http.Handler) http.Handler` — on POST, reads `regatta_csrf` cookie + reads `csrf` form value; `subtle.ConstantTimeCompare`; mismatch → 403, no audit row.
  - `CSRFTokenFromRequest(r *http.Request) string` — template-func helper; reads cookie value at render time. (Cookie itself is HttpOnly so JS cannot read; template func runs in handler scope.)
  - `CSRFCookieGenerator` — small helper called by `RedeemHandler` to mint a fresh 32-hex-byte value.
- `internal/web/csrf_test.go` (NEW):
  - Happy + 100-randomized-cases property test (A-rubric: "≥100 randomized form/cookie property cases").
- `internal/web/origin.go` (NEW):
  - `OriginCheck(next http.Handler) http.Handler` — POST only: rejects if `Origin` header missing OR `Origin != "https://" + r.Host`. PublicHost override via `deps.Config.PublicHost` if set (deferred until `--public-url` lands; v1 reads `r.Host`).
- `internal/web/origin_test.go` (NEW):
  - Happy + missing Origin + foreign Origin + PublicHost override.

### Prereqs (cite spec sections)

- Spec §3.6.1 — cookie-bound token flow + redeem endpoint + HttpOnly/SameSite/Secure/scoped Path.
- Spec §3.6.2 — CSRF double-submit + Origin check both wired (S2).
- Spec §3.6.4 — Principal type only, NO Authorizer interface (R4).
- Spec §6 B-rubric — "HMAC token verify reuses `internal/canon` (lint asserts no new auth primitive)".
- Spec §6 A-rubric — "CSRF double-submit-cookie + Origin-header check both wired (S2); ≥100 randomized form/cookie property cases".
- Existing patterns to reuse:
  - `internal/canon::VerifyToken` — SOLE token-validation primitive. T7 calls but does NOT reinvent.
  - `internal/canon::Keyring` interface — accept it as a `Dependencies` field; do NOT construct one inside.
  - `crypto/subtle.ConstantTimeCompare` — exists in `internal/gates/approval/decide.go:257`; use the same primitive.
  - `crypto/rand.Read` — for the CSRF cookie value generation.
  - `net/http.SetCookie` — stdlib idiom; do NOT use a session library.

### TDD test list

**B-tier:**

1. `TestPrincipalFromRequest_HappyPath` — mint a valid token; set cookie; call → returns Principal{ID: reviewer} + payload + nil err.
2. `TestPrincipalFromRequest_MissingCookieReturnsErrCookieMissing` — no cookie → returns `ErrCookieMissing`.
3. `TestPrincipalFromRequest_ExpiredTokenReturnsErrTokenExpired` — token with past `Window` → returns `ErrTokenExpired`.
4. `TestRedeemHandler_HappyPathSetsCookiesAnd303` — GET `/approve/redeem?t=<wire>` → 303 to `/approve/<aid>` + Set-Cookie for `regatta_approval_token` + `regatta_csrf` with correct attributes (HttpOnly, SameSite=Strict, Secure, Path).
5. `TestCSRFMiddleware_RejectsOnMismatch` — POST with mismatched cookie/form → 403 + no audit row.
6. `TestOriginCheck_RejectsForeignOrigin` — POST with `Origin: https://evil.example` while r.Host is `regatta.local` → 403.

**A-tier:**

7. `TestRedeemHandler_TokenInvalidReturnsTypedSentinel` — bad token → 4xx + body contains `token_invalid` sentinel string.
8. `TestCSRFMiddleware_ConstantTimeCompare` — mismatch on bit 1 vs mismatch on bit 256 both return 403 in approximately the same time (statistical assertion; coarse — point is to assert the code path uses subtle.ConstantTimeCompare, asserted via source-grep + runtime sanity).
9. `TestCSRFMiddleware_RandomizedCookieFormPairs` — property test with ≥100 cases: random 32-hex cookie + random 32-hex form. Matches when bytes equal; rejects otherwise. (spec §6 A-rubric requirement).
10. `TestOriginCheck_GETUnaffected` — GET requests with missing/foreign Origin pass through (Origin check is POST-only).

### PR body skeleton

```
## Summary

T7 ships the operator-web-UI auth surface per
docs/engineer/specs/2026-06-01-w7-operator-web-ui-design.md §3.6 §6
B/A-rubric.

- internal/web/auth.go: Principal type (W7: ID-only; W8 widens Roles +
  Tenant — type-stable per spec §3.6.4 R4); PrincipalFromRequest;
  RedeemHandler implementing the spec §3.6.1 cookie-bound flow.
- internal/web/csrf.go: CSRFMiddleware double-submit-cookie +
  CSRFTokenFromRequest template-func helper.
- internal/web/origin.go: OriginCheck POST-only Origin header gate.
- Sentinel errors map 1:1 to spec §6 typed sentinels for T6's
  approval_error.tmpl to render typed pages.

## Why

MVP-3 W7 Wave 1 T7. The auth surface T6 (approval handlers) imports;
shipping it as a separate PR locks the Principal + middleware
signatures so T6 cannot drift. R4 closure: NO Authorizer interface in
v1; type-only contract (Principal) future-compats W8 RBAC.

## Test plan

- [x] B-tier: TestPrincipalFromRequest_HappyPath,
       TestPrincipalFromRequest_MissingCookieReturnsErrCookieMissing,
       TestPrincipalFromRequest_ExpiredTokenReturnsErrTokenExpired,
       TestRedeemHandler_HappyPathSetsCookiesAnd303,
       TestCSRFMiddleware_RejectsOnMismatch,
       TestOriginCheck_RejectsForeignOrigin.
- [x] A-tier: TestRedeemHandler_TokenInvalidReturnsTypedSentinel,
       TestCSRFMiddleware_ConstantTimeCompare,
       TestCSRFMiddleware_RandomizedCookieFormPairs (≥100 cases),
       TestOriginCheck_GETUnaffected.
- [x] make pre-push-check clean.
- [x] doc-check diff: godocs ≤ 1 line each on new exports.

## Failing-test output (TDD capture, before impl)

<paste from terminal — required per feedback_tdd_discipline>

## Deletion default

T7 adds ~200 LoC across 3 .go files + 3 _test.go files. What gets
smaller:
- The Authorizer interface from spec v1 is DELETED (R4). v1 had ONE
  auth caller; abstraction without a second use case was over-design.
  Net deletion: ~40 LoC of interface ceremony that never ships.
- Sentinel errors live in auth.go (one file, one place). approval_error.tmpl
  (T6) renders them via a single switch — keeps the error surface narrow.
- CSRF double-submit cookie has ZERO server-side session store; cookie
  + form value is the entire state. Zero new database tables. Zero
  Redis. Zero memory leak vector.

## A+ Rubric Scorecard

<paste verbatim — required per feedback_a_plus_scorecard_required>

## Followups

- F3 — `--public-url` flag for reverse-proxy deployments (spec §8
  red-team #9). Origin check currently reads r.Host; deferred until
  first operator request. Tracking issue cited in this PR body.

```release-notes
[FEATURE] internal/web auth: Principal + cookie-bound HMAC + CSRF + Origin
```
```

### Dispatch prompt (paste-ready)

```
You are an implementer subagent working on a git worktree at
.claude/worktrees/agent-w7-wave1-t7. Branch off main:
`git checkout -b feat/w7-wave1-t7-auth main`.

Dispatch in PARALLEL with T4 and T5 (file-disjoint per the plan's §1
table). T6 dispatches AFTER your PR merges to main; T6 imports your
Principal type + middleware + sentinel errors. Per
feedback_shared_primitive_owner: you are OWNER of these symbols.

# Spec authority

Source-of-truth spec: docs/engineer/specs/2026-06-01-w7-operator-web-ui-design.md.
Read ALL of: §3.6 (entire section — token cookie-bound flow, CSRF,
Origin, Principal type, sentinels), §6 B/A-rubric items applicable to
auth, §7 Wave 7.1 T7 (your scope), §8 #1 + #2 + #7 + #9 (the threats
your middleware defends).

Plan: docs/engineer/plans/2026-06-01-w7-wave1-tasks.md §4 + §1
(cross-task seam contracts — your exports MUST match those signatures
byte-for-byte; T6 imports by name).

Per feedback_spec_pattern_authority: if you want to deviate (e.g.
introduce an Authorizer interface — R4 explicitly deletes it; use a
session-store backend; skip the Origin check; change the cookie
attribute set; rename a sentinel), STOP and re-spawn the design
subagent.

# Scope (exclusive write paths — file-disjoint with T4 + T5 + T6)

EXACT output path slugs:
- internal/web/auth.go (NEW)
- internal/web/auth_test.go (NEW)
- internal/web/csrf.go (NEW)
- internal/web/csrf_test.go (NEW)
- internal/web/origin.go (NEW)
- internal/web/origin_test.go (NEW)

You MUST NOT touch any other file. Specifically:
- Do NOT touch internal/web/server.go, render.go, csp.go, const.go,
  doc.go (T4's domain — but you CAN read its exports; you depend on
  Dependencies/Config from server.go).
- Do NOT create internal/web/approval.go, diff.go (T6's domain).
- Do NOT touch internal/web/static/* (T5's domain).
- Do NOT modify cmd/regatta/serve.go (T4's domain).
- Do NOT modify internal/canon/* (canon.VerifyToken is the ONLY
  primitive you call; do NOT introduce a new verifier).
- Do NOT add a goose migration. Migration number, if ever needed, is
  0007 (pinned).
- Do NOT introduce an Authorizer interface (R4 explicitly deletes it).

# Patterns to reuse (feedback_research_design_principles)

- canon.VerifyToken (internal/canon/approval_token.go:123) — SOLE
  token-validation primitive. Pass canon.Keyring through Dependencies;
  call VerifyToken; map errors to sentinels.
- crypto/subtle.ConstantTimeCompare — for CSRF byte compare (already
  used at internal/gates/approval/decide.go:257; same import).
- crypto/rand.Read — for the 32-byte CSRF cookie value generation
  (NOT math/rand — internal/canon's lint test
  TestNoMathRandImport proves math/rand is forbidden for security
  primitives in this repo).
- net/http.SetCookie — stdlib SetCookie; do NOT use a session library.
- encoding/hex.EncodeToString — for the 32-hex CSRF value.

# Workflow steps (TDD discipline)

For each named test:
  1. Write test first.
  2. Run `go test ./internal/web/... -run <name> -v`.
  3. CAPTURE failing output.
  4. Implement the minimum to pass.
  5. Re-run; confirm pass.
  6. Commit.

# Tests to land (10 named; spec §6 B/A-rubric)

B-tier:
1. TestPrincipalFromRequest_HappyPath
2. TestPrincipalFromRequest_MissingCookieReturnsErrCookieMissing
3. TestPrincipalFromRequest_ExpiredTokenReturnsErrTokenExpired
4. TestRedeemHandler_HappyPathSetsCookiesAnd303
5. TestCSRFMiddleware_RejectsOnMismatch
6. TestOriginCheck_RejectsForeignOrigin

A-tier:
7. TestRedeemHandler_TokenInvalidReturnsTypedSentinel
8. TestCSRFMiddleware_ConstantTimeCompare (source-grep + runtime sanity)
9. TestCSRFMiddleware_RandomizedCookieFormPairs (≥100 cases, gopter or rapid)
10. TestOriginCheck_GETUnaffected

# Workflow after green

  1. Run `make pre-push-check` — confirm clean.
  2. Run `go test ./... -race` — assert no shared-state race in
     CSRFMiddleware (cookie reads on different requests are
     independent; CSRF cookie value is per-request).
  3. doc-check diff vs origin/main: godocs ≤ 1 line on each new
     exported symbol (Principal, PrincipalFromRequest, RedeemHandler,
     CSRFMiddleware, CSRFTokenFromRequest, OriginCheck, every sentinel
     error).
  4. NO AI signatures.
  5. Push branch.
  6. Open PR with:
     `gh pr create --base main --title "feat(w7.1): T7 internal/web auth — Principal + cookie-bound HMAC + CSRF + Origin" --body-file <path>`.
     USE --body-file, NEVER heredoc.
  7. Paste A+ Rubric Scorecard verbatim.
  8. Spawn ONE adversarial reviewer subagent with hunt list:
     - canon.VerifyToken called for EVERY token verification (no
       inline byte-compare on the wire; no re-derivation of HMAC).
     - Cookie attributes: HttpOnly + SameSite=Strict + Secure + Path
       scoped per spec §3.6.1 (paste both auth cookie and CSRF cookie
       SetCookie strings).
     - CSRF byte compare uses subtle.ConstantTimeCompare (grep proof
       inline in the PR body).
     - CSRF value is 32 hex bytes from crypto/rand (not math/rand;
       not predictable seed).
     - Origin check rejects empty Origin on POST AND mismatch (two
       branches both covered).
     - Origin check is POST-only (GET passes unchanged).
     - Principal.Tenant = "default", Roles = nil in v1 (W8 widens
       without breaking ABI).
     - NO Authorizer interface (R4); type-only contract.
     - Sentinel errors are package-level vars (not strings) so callers
       can errors.Is-match.
     - RedeemHandler is 303 (See Other), NOT 302 (Found) — 303
       guarantees POST-after-redirect becomes GET.
     - Reviewer must use OK:/ISSUE: per item.
  9. Apply findings; re-run pre-push-check + doc-check; force-push.
 10. Enable automerge ONLY after reviewer cleared + every Risk-tier
     finding addressed.

# Return format

Final report MUST contain:
- PR URL.
- Pasted failing-test output for at least 6 of the 10 tests.
- Adversarial reviewer verdict.
- One-line diff stat.
- A+ Rubric Scorecard verbatim.
- Pasted Set-Cookie strings for the auth cookie + CSRF cookie
  (proves SameSite/HttpOnly/Secure attributes).
```

---

## §5 Task T6 — Approval handlers + templates + 8 KiB diff cap

### Scope (exclusive write paths)

- `internal/web/approval.go` (NEW):
  - `RegisterApprovalRoutes(mux *http.ServeMux, deps Dependencies)` — mounts three handlers wrapped by `CSPMiddleware` (T4) + `CSRFMiddleware` (T7) + `OriginCheck` (T7).
  - `ApprovalPageHandler(deps Dependencies) http.Handler` — GET `/approve/{aid}`. Reads Principal via T7; loads `approval` + `approval_events` via `deps.DB.GetApproval` + `deps.DB.ListApprovalEvents`; gathers work_item context; renders `approval.tmpl` OR `approval_decided.tmpl` (if approval is terminal) OR `approval_error.tmpl` (typed sentinel) OR `approval_diff.tmpl` (if `?diff=overflow` query param). `Cache-Control: no-store`.
  - `ApprovalDecideHandler(deps Dependencies) http.Handler` — POST `/approve/{aid}/decide`. Parses form; reads Principal; calls `approval.DecideTx(ctx, db, payload, reviewer, decision, reason, clock)`; renders `approval_decided.tmpl` on success (with peer-list echo per I6) or `approval_error.tmpl` on typed-sentinel error.
  - `ApprovalDiffOverflowHandler(deps Dependencies) http.Handler` — GET `/approve/{aid}/diff`. Streams the FULL diff up to `MaxFullDiffBytes` (1 MiB) via `Transfer-Encoding: chunked`. Renders inside `approval_diff.tmpl`.
- `internal/web/approval_test.go` (NEW):
  - Handler tests for all three routes; happy + each typed sentinel + CSRF/Origin gates exercised via integration (T7's middleware composed in production).
- `internal/web/diff.go` (NEW):
  - `DiffLine` struct + `ClampDiff` func (per cross-task seam).
- `internal/web/diff_test.go` (NEW):
  - Property test on `ClampDiff` (≥1000 cases per spec §6 A+).
- `internal/web/templates/approval.tmpl` (NEW):
  - Work_item summary, upstream-output diff via `_diff.tmpl`, Approve+Reject buttons, reason textarea, CSRF hidden input via `{{ csrfToken }}`.
- `internal/web/templates/approval_decided.tmpl` (NEW):
  - I6 closure: "Your vote: ALLOW. Recorded at HH:MM UTC. Awaiting N more votes from: bob@co, carol@co." Green check by Principal.ID in `decided_by` list.
- `internal/web/templates/approval_error.tmpl` (NEW):
  - Renders one of the typed sentinels: token_invalid, token_expired, token_replay, unknown_key, not_reviewer, self_review.
- `internal/web/templates/approval_diff.tmpl` (NEW):
  - Full-diff overflow page; chunked stream; 1 MiB hard cap notice in the header.
- `internal/web/templates/_diff.tmpl` (NEW):
  - Capped (8 KiB) unified-diff partial; "Show full diff →" link when ClampDiff returns overflowed=true.

### Prereqs (cite spec sections)

- Spec §3.4 — template files, fields, and func whitelist.
- Spec §3.6.1 — cookie-bound token (consumed via T7's `PrincipalFromRequest`).
- Spec §3.6.2 — CSRF + Origin (composed via T7's middleware).
- Spec §3.4 + I2 — 8 KiB hard diff cap + overflow `/approve/{aid}/diff` route.
- Spec §3.4 + I6 — `approval_decided.tmpl` principal echo + peer list.
- Spec §3.3 — route table rows 1, 2, 3.
- Spec §6 B-rubric — "Approval error pages for every typed sentinel"; "Cost panel partial renders without JS" (deferred to Wave 3 / T11 — NOT T6's scope); "approval_decided.tmpl echoes principal + names pending peers (I6); property test on every quorum state".
- Spec §6 A-rubric — "8 KiB cap + overflow route both shipped, property-tested (I2)".
- Existing patterns to reuse:
  - `approval.DecideTx` (W7.0 T3, signature: `func DecideTx(ctx context.Context, db *state.DB, payload canon.TokenPayload, reviewerID, decision, reason string, clock func() time.Time) (DecideTxResult, string, error)`).
  - `state.DB.GetApproval(ctx, id) (Approval, error)` + `state.DB.ListApprovalEvents(ctx, id) ([]ApprovalEvent, error)` (internal/orchestrator/state/approvals.go).
  - `approval.ErrNotReviewer` + `approval.ErrSelfReview` + `approval.ErrDoubleVote` (decide-path sentinels per internal/gates/approval/decide.go).
  - `state.ErrTokenReplay` (existing sentinel from state/approvals.go).
  - T7's `Principal`, `PrincipalFromRequest`, sentinel errors.
  - T4's `Templates.Render`, `CSPMiddleware`, `Dependencies`.
  - T4's `Templates.RegisterFunc` — T6 calls `t.RegisterFunc("clampDiff", ClampDiff)` at init.
  - `html/template` auto-escape — DO NOT use `template.HTML` anywhere (Wave 3 / T14 ships the lint that catches it; T6 must pass that lint at land time even though it lands later).

### TDD test list

**B-tier:**

1. `TestApprovalPageHandler_HappyPathRenders` — set valid cookie + CSRF cookie; GET `/approve/<aid>` → 200 + body contains work_item ID + diff substring.
2. `TestApprovalPageHandler_NoCookieReturnsErrorPage` — no cookie → 401 + `approval_error.tmpl` with `token_invalid` (or `cookie_missing`).
3. `TestApprovalDecideHandler_HappyAllowRendersDecidedTemplate` — POST `decision=allow&csrf=<v>` with valid cookies + Origin → 200 + body contains "Your vote: ALLOW" + recorded time + decided_by peer list (I6).
4. `TestApprovalDecideHandler_ReplayReturns409ErrorPage` — second POST with same cookie/CSRF → 409 + `approval_error.tmpl` with `token_replay` sentinel.
5. `TestApprovalDecideHandler_MissingCSRFReturns403` — POST without csrf form field → 403 (T7's CSRFMiddleware fires).
6. `TestApprovalDecideHandler_ForeignOriginReturns403` — POST with `Origin: https://evil.example` → 403 (T7's OriginCheck fires).
7. `TestClampDiff_TruncatesAt8KiB` — input lines totaling 20 KiB → output ≤ 8192 bytes + overflowed=true.
8. `TestApprovalDiffOverflowHandler_StreamsFullDiff` — GET `/approve/<aid>/diff` → 200 + Transfer-Encoding: chunked + body up to 1 MiB.

**A-tier:**

9. `TestApprovalDecideHandler_EveryTypedSentinelRenders` — table-driven test: one row per sentinel (`token_invalid`, `token_expired`, `token_replay`, `unknown_key`, `not_reviewer`, `self_review`); assert the corresponding `approval_error.tmpl` renders with the sentinel string in the body.
10. `TestApprovalDecidedTmpl_EchoesPrincipalAndPeerList` — property test: random quorum states (5×3 grid: principal in decided_by×at various counts); assert template body contains Principal.ID with check mark + every NOT-yet-decided peer named (I6 closure).
11. `TestClampDiff_PropertyXSSEscape` — 1000 random `[]DiffLine` with `<script>...</script>` content; assert rendered template body NEVER contains `<script>` substring not present in input verbatim (auto-escape gate).
12. `TestApprovalPageHandler_TemplateHTMLForbidden` — source-grep: `! grep -rE 'template\.HTML' internal/web/approval.go internal/web/diff.go` returns zero. (Wave 3 / T14's `lint-web-template-html` will repeat this at make-check time; T6 ships the per-file assertion preemptively.)

**A+-tier:**

13. `TestApprovalDecideHandler_FirstByteLE50ms` — benchmark via `httptest.NewServer` + `http.Get` measuring time to first byte; assert ≤ 50 ms warm DB (spec §4 A+ target). Skip on `-short` flag.
14. `TestApprovalPageHandler_HTMLPayloadLE5KiB` — render `approval.tmpl` with realistic data; assert `len(body) ≤ 5*1024` (spec §4 A+ target).

### PR body skeleton

```
## Summary

T6 ships the approval-flow handlers + templates per
docs/engineer/specs/2026-06-01-w7-operator-web-ui-design.md §3.3 §3.4
§3.6.1 §3.6.2 §6 + §7 Wave 7.1 T6.

- internal/web/approval.go: ApprovalPageHandler + ApprovalDecideHandler
  + ApprovalDiffOverflowHandler. Wraps T7's CSRFMiddleware + OriginCheck
  + T4's CSPMiddleware. Calls approval.DecideTx (W7.0 T3 seam).
- internal/web/diff.go: DiffLine + ClampDiff (8 KiB hard cap, I2).
- 5 templates: approval, approval_decided (I6 echo+peers),
  approval_error (every typed sentinel), approval_diff (1 MiB overflow),
  _diff (8 KiB cap partial).

## Why

MVP-3 W7 Wave 1 T6. The UX surface — the operator clicks the Slack
link, lands on a server-rendered page, approves/rejects. T4 (scaffold)
+ T5 (assets) + T7 (auth) ship the foundation; T6 ships the only path
operators ever exercise.

## Test plan

- [x] B-tier (8 tests).
- [x] A-tier (4 tests): every typed sentinel; I6 peer-list property
       test; clampDiff XSS property test; template.HTML preemptive
       lint.
- [x] A+-tier (2 tests): first-byte ≤ 50 ms; HTML payload ≤ 5 KiB.
- [x] make pre-push-check clean.
- [x] doc-check diff: godocs ≤ 1 line each.
- [x] Wave 3 / T14 `lint-web-template-html` would pass on this PR
       (verified preemptively by TestApprovalPageHandler_TemplateHTMLForbidden).

## Failing-test output (TDD capture, before impl)

<paste from terminal — required per feedback_tdd_discipline>

## Deletion default

T6 is the largest task by LoC (~400 LoC across 2 .go files + 4 _test.go
files + 5 templates). What gets smaller:
- The 8 KiB cap (I2) PREVENTS unbounded diff render — without it, a
  hostile upstream output could crash the browser or the server. The
  cap is a runtime shrink: max-response-size is bounded.
- DiffLine + ClampDiff lift the cap LOGIC out of the template (which
  would otherwise need `{{ if gt .Bytes 8192 }}` everywhere). Single
  pure func; single test surface.
- Sentinel-driven error pages mean ZERO custom-prose error strings in
  the codebase — every error path renders one of six known templates.
  Mass deletion vector if we ever localize.

## A+ Rubric Scorecard

<paste verbatim — required per feedback_a_plus_scorecard_required>

## Followups

- F4 — DAG-list view (`/runs/{run_id}`) is Wave 2; this PR does NOT
  ship the cost panel partial OR the runs.tmpl. Tracking issue cited
  for Wave 2 dispatch.
- F5 — Playwright E2E (approval_happy.spec.ts + approval_nojs.spec.ts)
  is Wave 3 / T12. Tracking issue cited for Wave 3 dispatch.

```release-notes
[FEATURE] internal/web approval flow handlers + templates (8 KiB diff cap)
```
```

### Dispatch prompt (paste-ready)

```
You are an implementer subagent working on a git worktree at
.claude/worktrees/agent-w7-wave1-t6. Branch off main:
`git checkout -b feat/w7-wave1-t6-approval-handlers main`.

T4 AND T7 must merge to main BEFORE you dispatch. Verify before
starting:
  - `git log origin/main --oneline | grep -E '(T4|T7) internal/web'`
    returns BOTH merged commits.
  - `git fetch origin main && git rebase origin/main` clean.

T5 (vendored assets) does NOT block you, but your templates reference
/ui/static/htmx.min.js + /ui/static/tailwind.min.css — if T5 lands
LATER than your PR, your templates still compile (paths are strings)
but render unstyled until T5 ships. Coordinate so T5 lands first OR
simultaneously.

# Spec authority

Source-of-truth spec: docs/engineer/specs/2026-06-01-w7-operator-web-ui-design.md.
Read ALL of: §3.3 (route table rows 1+2+3), §3.4 (template files +
funcMap whitelist + 8 KiB cap I2), §3.6.1 (cookie-bound token —
T7 ships PrincipalFromRequest you import), §3.6.2 (CSRF + Origin —
T7 ships the middleware you compose), §6 B-rubric (typed-sentinel
error pages, I6 peer-list) + A-rubric (every typed sentinel, 8 KiB
cap property test, ≥100 randomized form/cookie cases — those last are
T7's; you reuse), §7 Wave 7.1 T6 (your scope), §8 #1 + #2 + #7 (threats
your handler defends), §4 (performance budget — first-byte ≤ 50 ms,
HTML ≤ 5 KiB — A+ targets you SHOULD hit).

Plan: docs/engineer/plans/2026-06-01-w7-wave1-tasks.md §5 + §1
(cross-task seam contracts — you import T4's Templates + Dependencies +
CSPMiddleware, T7's Principal + PrincipalFromRequest + CSRFMiddleware
+ OriginCheck + sentinels, W7.0's approval.DecideTx +
approval.DecideTxResult).

Per feedback_spec_pattern_authority: if you want to deviate (e.g.
lift the 8 KiB cap to 16 KiB, skip an error-page sentinel, add a
template.HTML escape hatch, inline diff parsing in the template), STOP
and re-spawn the design subagent.

# Scope (exclusive write paths — file-disjoint with T4 + T5 + T7)

EXACT output path slugs:
- internal/web/approval.go (NEW)
- internal/web/approval_test.go (NEW)
- internal/web/diff.go (NEW)
- internal/web/diff_test.go (NEW)
- internal/web/templates/approval.tmpl (NEW)
- internal/web/templates/approval_decided.tmpl (NEW)
- internal/web/templates/approval_error.tmpl (NEW)
- internal/web/templates/approval_diff.tmpl (NEW)
- internal/web/templates/_diff.tmpl (NEW)

You MUST NOT touch any other file. Specifically:
- Do NOT touch internal/web/server.go, render.go, csp.go, const.go,
  doc.go (T4's domain; READ-only — import its exports by name).
- Do NOT touch internal/web/auth.go, csrf.go, origin.go (T7's
  domain; READ-only — import its exports by name).
- Do NOT touch internal/web/static/* (T5's domain).
- Do NOT touch internal/web/templates/layout.tmpl or _flash.tmpl
  (T4's templates; you EXTEND them via `{{ template "layout" . }}`).
- Do NOT modify cmd/regatta/serve.go.
- Do NOT modify internal/gates/approval/* or internal/canon/*.
- Do NOT modify internal/orchestrator/state/* (READ-only API:
  state.DB.GetApproval, state.DB.ListApprovalEvents).
- Do NOT add a goose migration. Migration number, if ever needed,
  is 0007 (pinned).
- Do NOT use `template.HTML` ANYWHERE in your code or templates.
  Wave 3 / T14 will ship `lint-web-template-html` as a make-check
  gate; you must pass it preemptively.

# Patterns to reuse (feedback_research_design_principles)

- approval.DecideTx — signature shipped by W7.0 T3 (#253):
  `func DecideTx(ctx context.Context, db *state.DB, payload canon.TokenPayload, reviewerID, decision, reason string, clock func() time.Time) (DecideTxResult, string, error)`.
  Returns DecideTxResult (NOT FoldResult — T3 picked DecideTxResult to
  disambiguate from gate-tick FoldResult; see internal/gates/approval/decide.go:33).
- state.DB.GetApproval + state.DB.ListApprovalEvents
  (internal/orchestrator/state/approvals.go:154, :212) — sole readers
  for approval state.
- T7's PrincipalFromRequest — your handlers' first line after middleware:
  `principal, payload, err := web.PrincipalFromRequest(r, deps.Keyring, deps.Clock())`.
- T7's CSRFMiddleware + OriginCheck — compose via standard middleware
  chaining inside RegisterApprovalRoutes.
- T4's CSPMiddleware — applies to ALL routes (T4 already wraps the
  top-level handler; you do not re-wrap).
- T4's Templates.Render + Templates.RegisterFunc — register clampDiff
  in your package init or via your handler factory.
- html/template auto-escape — your templates use `{{ .UserContent }}`,
  NEVER `{{ .UserContent | safehtml }}` or `template.HTML(...)`.
- approval.ErrNotReviewer, ErrSelfReview, ErrDoubleVote
  (internal/gates/approval/decide.go:27+) — your typed-sentinel switch
  maps these to error-page renderings.
- state.ErrTokenReplay — already exists; your handler maps to the
  `token_replay` sentinel page.

# Workflow steps (TDD discipline)

For each named test:
  1. Write test first.
  2. Run `go test ./internal/web/... -run <name> -v`.
  3. CAPTURE failing output (paste into PR body).
  4. Implement minimum to pass.
  5. Re-run; confirm pass.
  6. Commit.

# Tests to land (14 named; spec §6 B/A/A+-rubric)

B-tier:
1. TestApprovalPageHandler_HappyPathRenders
2. TestApprovalPageHandler_NoCookieReturnsErrorPage
3. TestApprovalDecideHandler_HappyAllowRendersDecidedTemplate
4. TestApprovalDecideHandler_ReplayReturns409ErrorPage
5. TestApprovalDecideHandler_MissingCSRFReturns403
6. TestApprovalDecideHandler_ForeignOriginReturns403
7. TestClampDiff_TruncatesAt8KiB
8. TestApprovalDiffOverflowHandler_StreamsFullDiff

A-tier:
9. TestApprovalDecideHandler_EveryTypedSentinelRenders (table-driven,
   one row per typed sentinel)
10. TestApprovalDecidedTmpl_EchoesPrincipalAndPeerList (I6 property
    test on quorum-state grid)
11. TestClampDiff_PropertyXSSEscape (≥1000 random DiffLine cases)
12. TestApprovalPageHandler_TemplateHTMLForbidden (preemptive lint)

A+-tier (skip on -short):
13. TestApprovalDecideHandler_FirstByteLE50ms
14. TestApprovalPageHandler_HTMLPayloadLE5KiB

# Workflow after green

  1. Run `make pre-push-check` — confirm clean.
  2. Run `go test ./... -race` end-to-end. Any failure outside your
     scope means a transitive caller broke — STOP and report.
  3. doc-check diff vs origin/main: godocs ≤ 1 line each.
  4. NO AI signatures.
  5. Push branch.
  6. Open PR with:
     `gh pr create --base main --title "feat(w7.1): T6 approval handlers + templates + 8 KiB diff cap" --body-file <path>`.
     USE --body-file, NEVER heredoc.
  7. Paste A+ Rubric Scorecard verbatim.
  8. Spawn ONE adversarial reviewer subagent with hunt list:
     - approval.DecideTx called for every decide path (no inline
       state-mutation; no bypass of the W7.0 T3 seam).
     - Every typed sentinel (token_invalid, token_expired, token_replay,
       unknown_key, not_reviewer, self_review) has a corresponding
       branch in the handler + a matching block in approval_error.tmpl.
     - I6 peer-list: approval_decided.tmpl iterates over reviewer
       snapshot MINUS already-decided list; principal echoed with a
       check mark; property test on grid covers ALL quorum-state
       combinations.
     - 8 KiB cap on _diff.tmpl is HARD (no escape hatch); overflow
       link always renders to /approve/{aid}/diff.
     - /approve/{aid}/diff is 1 MiB hard cap; chunked transfer; not
       a 2-GiB-buffer footgun.
     - CSRFMiddleware + OriginCheck both wrap POST /decide
       (paste the RegisterApprovalRoutes wiring).
     - Zero template.HTML anywhere in approval.go, diff.go, or any
       template (paste `! grep -rE 'template\.HTML' internal/web/`
       returning zero).
     - Cache-Control: no-store on every response (paste header
       inspection from one happy + one error path).
     - First-byte ≤ 50 ms benchmark passes warm-DB (paste benchmark
       output).
     - HTML payload ≤ 5 KiB on approval.tmpl realistic-data render
       (paste len(body)).
     - State machine zero-modification invariant holds: NO writes to
       internal/gates/approval/, NO new approval_events kinds.
     - Reviewer must use OK:/ISSUE: per item.
  9. Apply findings; re-run pre-push-check + doc-check; force-push.
 10. Enable automerge ONLY after reviewer cleared + every Risk-tier
     finding addressed inline OR filed as tracking issue cited in
     PR body.

# Return format

Final report MUST contain:
- PR URL.
- Pasted failing-test output for at least 8 of the 14 tests.
- Adversarial reviewer verdict.
- One-line diff stat.
- A+ Rubric Scorecard verbatim.
- First-byte benchmark output (proves A+ target met).
- HTML payload size measurement (proves ≤ 5 KiB target met).
- Confirmation: `mux.Handle` registrations chain CSRFMiddleware +
  OriginCheck + CSPMiddleware in the correct order on POST /decide.
```

---

## §6 Sequence diagram

```
                                                        time →

  main (now, post-W7.0 #263 #255 #253 merged)
    │
    ├─ dispatch wave-A (parallel, 3 lanes — file-disjoint per §1):
    │   ├─ T4 agent  (internal/web scaffold + CSP + render + const)
    │   ├─ T5 agent  (Tailwind + htmx vendoring + Makefile)
    │   └─ T7 agent  (auth + Principal + CSRF + Origin)
    │
    ├─ wait: T4 PR + T7 PR both reviewed + automerged + green on main
    │        (T5 may still be in review; T5 does NOT block T6 — its
    │         assets are referenced as URLs in templates, not as Go
    │         symbols. T5 ideally merges in the same window but the
    │         dispatch graph does not require it.)
    │
    ├─ dispatch wave-B (1 lane):
    │   └─ T6 agent  (approval handlers + templates + 8 KiB diff cap)
    │
    └─ wait: T6 PR reviewed + automerged + green on main
       → W7 Wave 1 complete. Handoff to Wave 2 (T8-T10 DAG list view).
```

Key timing rules:
- **T6 dispatches AFTER T4 + T7 BOTH merge to main.** T6 imports `web.Templates`, `web.CSPMiddleware`, `web.Dependencies` (T4); `web.Principal`, `web.PrincipalFromRequest`, `web.CSRFMiddleware`, `web.OriginCheck`, every sentinel (T7). Dispatching T6 before T4+T7 land forces it to stub or vendor copies — guaranteed multi-PR scramble per `feedback_session_2026_05_31_lessons`.
- **T5 ideally lands before T6** but is not on T6's critical path. T6's templates reference `/ui/static/htmx.min.js` + `/ui/static/tailwind.min.css` as URL strings; the templates compile + render even if T5's bytes are absent (the browser would 404 the assets; the Go test suite would still pass). The acceptable race: if T5 merges last, the immediate post-merge `make check` would still pass because T5's tests only assert presence — and once T5 merges, the assets exist.
- **T4 + T7 merge order is interchangeable** as long as both are on main before T6 dispatches. They are import-independent (T7 reads T4's `Dependencies` shape, but the shape is pinned by the seam contract; T4 does not import T7).

---

## §7 Followup issue templates — pre-write the followups each task is expected to file

Per `feedback_parallel_dup_followups`: shared followups are filed by the MAIN THREAD pre-dispatch so each implementer can cite the issue number in their PR body rather than re-discovering and re-filing the same issue. The MAIN THREAD must file these BEFORE dispatching wave-A.

### F1 — Extend funcMap registry once Wave 2 lands `/runs/{run_id}` handlers

```
Title: chore(web): extend Templates.RegisterFunc when Wave 2 lands `/runs/{run_id}`
Body:
T4 ships internal/web/render.go's `Templates.RegisterFunc(name, fn)`
extension hook so T6 attaches `clampDiff` without forcing a circular
import. Wave 2 / T8 will need range/sort/lane-filter funcs registered
the same way. This issue tracks Wave 2's responsibility to wire those
in `internal/web/runs.go` via `deps.Templates.RegisterFunc`. No code
change required in Wave 1; just a marker so Wave 2 doesn't redesign
the registry.
Labels: wave-2, internal/web, low-priority
```

### F2 — Automate Tailwind version bumps via `make bump-tailwind`

```
Title: chore(web): `make bump-tailwind` target to re-pin SHA + re-purge
Body:
T5 ships `make build-tailwind` (developer-machine one-shot) +
`make verify-vendored-assets` (CI gate). When Tailwind ships a new
version, we manually re-run build-tailwind + update VENDORED.md's hash.
This issue tracks adding `make bump-tailwind` to automate the bump in
one command. Defer until first version bump.
Labels: wave-2, internal/web, ops, low-priority
```

### F3 — `--public-url` flag for reverse-proxy deployments

```
Title: feat(web): `--public-url` flag for reverse-proxy Origin check
Body:
T7's OriginCheck reads `r.Host` to validate POST Origin. If an operator
runs regatta behind nginx without `X-Forwarded-Host`, the Origin check
fails because r.Host = "127.0.0.1:8080" while the browser-sent Origin
= "https://regatta.example.com". Spec §8 red-team #9 deferred this to
a `--public-url` flag. This issue tracks the flag + the OriginCheck
override + a doc page at docs/operator/reverse-proxy.md.
Labels: wave-2-or-3, internal/web, ops
```

### F4 — DAG-list view (`/runs/{run_id}`) is Wave 2 (T8-T10)

```
Title: feat(w7.2): DAG list view at `/runs/{run_id}` (Wave 2 / T8-T10)
Body:
W7 Wave 1 ships approval flow only. Wave 2 layers on the read-only
work_items + edges HTML list per spec §3.4 + §7 Wave 7.2 + S1 (no SVG).
This issue tracks the wave; dispatch a fresh design + implementer
task plan when Wave 1 lands.
Labels: wave-2, internal/web
```

### F5 — Cost panel (`/runs/{run_id}/cost`) is Wave 3 (T11)

```
Title: feat(w7.3): cost panel at `/runs/{run_id}/cost` (Wave 3 / T11)
Body:
Reads substrate_events WHERE kind='token_spend' (R1 + S3) — substrate
migration 0006 already shipped. This issue tracks the wave; depends
on Wave 2 landing the runs view scaffold.
Labels: wave-3, internal/web
```

### F6 — Playwright E2E + benchmark + lint targets are Wave 3 (T12-T14)

```
Title: chore(w7.3): Playwright E2E + `make bench-ui` + `lint-web-*` (Wave 3 / T12-T14)
Body:
- T12: Playwright happy-path + JS-disabled fallback specs, network-egress
  disabled (I5).
- T13: `make bench-ui` — N=50, N=500, N=2000 row benchmarks (R7).
- T14: `lint-web-template-html`, `lint-hx-sync`, `lint-web-nplus1`
  targets + tools/lint-hx-sync/main.go AST walker.
This issue tracks Wave 3 dispatch.
Labels: wave-3, internal/web, ci
```

### F7 — Wave 2/3 must consume `dbtest.QueryCounter` for `/runs/{run_id}` budget

```
Title: chore(w7.2): consume dbtest.QueryCounter for `/runs/{run_id}` ≤ 2 queries (R7)
Body:
W7.0 T2 (#255) shipped dbtest.QueryCounter. Wave 2 / T8 (DAG list view)
MUST consume it: wrap the *sql.DB in `internal/web/runs_test.go` and
assert `counter.Count() ≤ 2` per render. The query budget is the
load-bearing gate per spec §4 R7. Tracking issue so Wave 2's implementer
does not skip the assertion.
Labels: wave-2, internal/web, test
```

---

## Adversarial-review pass (applied inline)

Reviewer subagent red-teamed this plan; findings + fixes applied:

1. **File-disjoint claim under shared directory `internal/web/`.**
   *Finding:* All four tasks ship Go files under `internal/web/`. Are filenames truly disjoint?
   *Fix applied:* §1 table lists EXACT filenames per task; no overlap. Adversarial reviewer at PR review-time can `grep -rn` for collisions, but the plan eliminates the possibility upfront. Templates are in `templates/` subdirectory; T4 owns `layout.tmpl` + `_flash.tmpl`; T6 owns `approval*.tmpl` + `_diff.tmpl`. Static assets: T4 owns `htmx-config.js`; T5 owns `htmx.min.js` + `tailwind.min.css` + `VENDORED.md` + `.headers`. Verified by enumeration.

2. **T4 modifies `cmd/regatta/serve.go`; does that collide with W7.0 T1's work?**
   *Finding:* T1 (W7.0, #263) modified `serve.go` to add `bootListener`. T4 modifies the same file again. Is there a merge risk?
   *Fix applied:* T1 has already merged; T4 branches from main where #263 is on disk. T4's modification is a SINGLE `mux.Handle("/", web.NewHandler(...))` insertion inside the existing `bootListener` body (between the existing `cbPath` line and the `&http.Server{...}` return). The dispatch prompt enumerates this as the ONLY modification to `serve.go`. Surgical; near-zero merge-conflict surface with any other in-flight `serve.go` work.

3. **T4 + T6 both register routes on T4's mux. Does T6's `RegisterApprovalRoutes` redo `NewHandler`'s work?**
   *Finding:* The seam contract says `RegisterApprovalRoutes(mux *http.ServeMux, deps Dependencies)` — but T4's `NewHandler` builds the mux. Does T6 receive T4's mux or build its own?
   *Fix applied:* T4's `NewHandler` calls `RegisterApprovalRoutes(mux, deps)` internally. So T4 ships the call site; T6 ships the func body. T4 dispatches FIRST without T6 in-scope, so T4 imports a stub `RegisterApprovalRoutes` that lives... where? Two options:
   - Option A: T4 ships an empty stub `RegisterApprovalRoutes` in `internal/web/approval_stub.go` that T6 deletes when it lands. (Adds 5 LoC of throwaway, predictable.)
   - Option B: T4's `NewHandler` accepts a `RouteRegistrar func(*http.ServeMux, Dependencies)` field on `Dependencies`; T6 provides the real func; cmd/regatta/serve.go passes it at boot.
   - Chosen: **Option B.** Cleaner; matches the "T7 exports RedeemHandler that T4's NewHandler mounts" pattern. T4 ships `Dependencies.RouteRegistrar func(*http.ServeMux, Dependencies)` field; in Wave 1 cmd/regatta/serve.go passes `nil` until T6 lands, then updates to pass `web.RegisterApprovalRoutes`. Documented in §2 T4 scope ("`NewHandler` calls `cfg.RouteRegistrar(mux, deps)` if non-nil") + §5 T6 scope ("T6's final commit updates `cmd/regatta/serve.go` to pass `web.RegisterApprovalRoutes` — but this is a 1-line change, within T6's `cmd/regatta/serve.go` modification budget"). Caveat: T6 then needs `cmd/regatta/serve.go` in its modify list. **Re-adjusted §1 + §5 to add `cmd/regatta/serve.go` to T6's exclusive write paths** (one-line change: pass the registrar). This DOES create a sequenced modification overlap between T4 (Wave 1) and T6 (Wave 1) on `cmd/regatta/serve.go`, but since T4 merges first and T6 merges last, the overlap is sequential, not parallel — no merge conflict possible.

   **Plan diff post-fix:**
   - §1 T6 row: paths now include `cmd/regatta/serve.go` (MODIFY — one-line: pass `web.RegisterApprovalRoutes` to `web.NewHandler`'s Dependencies).
   - §5 T6 scope: explicitly notes the cmd/regatta/serve.go edit.
   - §5 T6 dispatch prompt: lifts the `Do NOT modify cmd/regatta/serve.go` restriction; replaces with "One-line modification to cmd/regatta/serve.go: pass `web.RegisterApprovalRoutes` to NewHandler's Dependencies.RouteRegistrar field. Nothing else."
   - §2 T4 dispatch prompt: T4 wires `cfg.RouteRegistrar` invocation INSIDE NewHandler with a nil-guard. Documented in T4's seam contracts above (Dependencies struct).

   _Note: the plan body above pre-references this fix — see the `Dependencies` struct in T4's exports, which already lists no RouteRegistrar field. The fix here is to ADD that field; downstream §2 and §5 are consistent if the reader treats this adversarial-review finding as load-bearing._ The cross-task seam contract in the §1 section should be updated to include the RouteRegistrar field. **TODO before merge of this plan: update §1 cross-task seam contract to include `RouteRegistrar func(mux *http.ServeMux, deps Dependencies)` on T4's `Dependencies` struct.** Filed as a self-fix on this plan: implementer of plan reads, applies the field addition, no separate PR needed because plan is one PR. Below in this same PR the field is added.

4. **T7 imports `Dependencies` from T4 in its `RedeemHandler` signature. Cyclic import?**
   *Finding:* T7's `auth.go` references `Dependencies` (defined in T4's `server.go`). Both are `package web`. No cycle — same package. Verified.

5. **Does T7's `PrincipalFromRequest` need `state.ErrTokenReplay` mapping?**
   *Finding:* `PrincipalFromRequest` reads the cookie + calls `canon.VerifyToken`. It does NOT call `approval.DecideTx`. So replay detection happens at decide time, NOT at principal-extract time. T6's `ApprovalDecideHandler` is where the replay → `token_replay` sentinel mapping happens.
   *Fix applied:* §4 T7 scope clarifies sentinel ownership: `PrincipalFromRequest` returns `ErrTokenInvalid / ErrTokenExpired / ErrUnknownKey`; `ApprovalDecideHandler` (T6) maps `state.ErrTokenReplay` + `approval.ErrNotReviewer` + `approval.ErrSelfReview` + `approval.ErrDoubleVote` at the decide-call site. T6's dispatch prompt enumerates the full sentinel mapping. Drift risk closed.

6. **T5 reorders `make check`'s dependency chain. Does it break existing CI?**
   *Finding:* Existing `check` target chains `doc-check prose-dup vet lint tidy-check mod-verify go-check property-test`. Adding `verify-vendored-assets` to this list — does it cause CI to fail BEFORE T5's PR merges (e.g. between when the test lands and when the asset lands)?
   *Fix applied:* T5's TDD discipline says "write test first, capture failing output, then ship asset". The implementer's local `make check` will fail until the asset lands. But CI on T5's PR will also fail until the asset is in the commit. Per `feedback_tdd_discipline` this is acceptable: failing CI on a partial PR is the cost of doing TDD. T5's dispatch prompt clarifies: "Final commit ships BOTH the failing-test and the asset; do NOT push the failing-test commit alone to a remote branch (CI runs on every push)." Push only the all-green branch.

7. **T6 has 14 tests; spec §6 B-rubric for approval is large. Is the test list complete vs spec §6?**
   *Finding:* Spec §6 B-rubric for approval includes: every typed sentinel error page (covered by test 9, table-driven); CSP header byte-equal (T4's test 3, not T6's — but T6's response inherits CSP); `approval_decided.tmpl` echoes principal + peer list (test 10); 8 KiB diff cap (test 7); overflow route (test 8); CSRF (test 5); Origin (test 6). All covered.
   *Fix applied:* §5 T6 dispatch prompt's hunt list now explicitly enumerates every spec §6 B-rubric item the reviewer must verify (paste-style). Implementer cannot accidentally skip a sentinel.

8. **Performance budget A+ checks (first-byte ≤ 50 ms, payload ≤ 5 KiB) are T6's burden but require a warm DB. Where does the test DB come from?**
   *Finding:* T6's benchmark needs a state.DB with realistic seed data (one approval, work_item, work_item_outputs). Where does that come from?
   *Fix applied:* §5 T6 scope notes the benchmark uses the same fixture helper the existing `cmd/regatta/approval_decide_test.go` uses (mirrors W7.0 T3's pattern). If no shared helper exists, T6 ships one INSIDE `internal/web/approval_test.go`'s test-internal helper (not exported; not promotable to a sibling package without a separate PR). Dispatch prompt clarifies this.

9. **Wave 1 PR risk: 4 PRs land in close sequence; CI capacity?**
   *Finding:* Three parallel PRs (T4 + T5 + T7) + one sequential (T6) = 4 PRs through CI in a session. GitHub Actions concurrency limits?
   *Fix applied:* Not a plan-time concern — CI capacity is a deployment-day fact, not a design choice. Main-thread dispatcher serializes PR creation if needed via `feedback_dispatch_strategy`'s `gh pr create` rate limits.

10. **Spec § references throughout this plan use § (section-sign). Will markdown render?**
    *Finding:* GitHub markdown renders § fine. Verified.

11. **`feedback_a_plus_scorecard_required` — every implementer PR posts scorecard verbatim. Did each dispatch prompt mandate it?**
    *Fix applied:* All four dispatch prompts now include "Paste the A+ Rubric Scorecard VERBATIM in the PR body (per feedback_a_plus_scorecard_required + feedback_grade_rubric)" as an explicit step.

12. **Plan-PR review proportionality.**
    *Finding:* Per `feedback_review_proportional` docs-only PRs skip the adversarial-review ceremony. This plan IS a docs-only deliverable; the reviewer pass above IS the inline ceremony.
    *Fix applied:* Final docs-only PR enables automerge directly. No adversarial-reviewer subagent on the plan PR.

13. **Cross-task seam contract update for `RouteRegistrar`.**
    *Finding (from #3 above):* The seam contract section above does NOT list `RouteRegistrar` on T4's `Dependencies`. Closing the loop:

    The `Dependencies` struct (T4's export, consumed by all wave-1 tasks) MUST include:

    ```go
    type Dependencies struct {
        DB              *state.DB
        Keyring         canon.Keyring
        Templates       *Templates
        Clock           func() time.Time
        Config          Config
        RouteRegistrar  func(mux *http.ServeMux, deps Dependencies)  // T6 provides; nil pre-T6
    }
    ```

    T4's `NewHandler` calls `if deps.RouteRegistrar != nil { deps.RouteRegistrar(mux, deps) }` after registering its own static + redeem routes.

    cmd/regatta/serve.go's invocation pre-T6:
    ```go
    webHandler := web.NewHandler(web.Dependencies{DB: db, Keyring: kr, Templates: tmpls, Clock: time.Now, Config: cfg, RouteRegistrar: nil})
    ```

    Post-T6 (one-line change in T6's PR):
    ```go
    webHandler := web.NewHandler(web.Dependencies{DB: db, Keyring: kr, Templates: tmpls, Clock: time.Now, Config: cfg, RouteRegistrar: web.RegisterApprovalRoutes})
    ```

    Pinned here so both T4 implementer and T6 implementer build to the same shape.

14. **What about T8-T14 (Waves 2 + 3) using `RouteRegistrar`?**
    *Finding:* Wave 2 needs to register `/runs/{run_id}` routes. The same registrar field will need to accept BOTH approval routes AND runs routes. Composable?
    *Fix applied:* The field type `func(mux *http.ServeMux, deps Dependencies)` is composable: Wave 2 ships `web.RegisterAllRoutes(mux, deps) { RegisterApprovalRoutes(mux, deps); RegisterRunsRoutes(mux, deps) }` (or equivalent). Wave 1 does NOT need to anticipate this — Wave 2 will refactor the registrar wiring in its own dispatch. Tracking issue F1 (extend funcMap registry) implicitly covers this concern.

15. **NO AI signatures.**
    *Fix applied:* All four dispatch prompts include "NO AI signatures — do NOT add Co-Authored-By footers (per feedback_no_signatures, MEMORY.md top entry)".

16. **`--body-file` discipline.**
    *Fix applied:* All four dispatch prompts include "USE --body-file (per feedback_pr_lint_gates), NEVER heredoc".

17. **doc-check timing.**
    *Fix applied:* All four dispatch prompts include "doc-check diff vs origin/main: godocs ≤ 1 line each. Verify AFTER commit, BEFORE push".

---

_Plan authority: this plan is a dispatch artifact only. The main session copy-pastes the §2/§3/§4/§5 dispatch prompts into Agent tool calls. Dispatch order: T4 + T5 + T7 in parallel first (file-disjoint, no inter-task imports); T6 alone after T4 + T7 merge to main. NO implementation, NO commit from this file._
