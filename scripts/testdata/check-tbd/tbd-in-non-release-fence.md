# Spec with bare TBD inside a non-release-notes code fence

The release-notes fence is the only operator-scratch exemption. Other fences
are spec content and the gate enforces resolution.

```bash
echo "owner=TBD"
```

Body prose stays clean otherwise.
