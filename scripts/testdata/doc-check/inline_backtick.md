# Inline-backtick fixture

A doc that names banned tokens inline. Must pass the banned-phrase gate
because the tokens appear as code spans, not as marketing claims.

The banned tokens include `robust`, `blazing-fast`, and `production-grade`.
A reword tip: replace `world-class` with a falsifiable metric.
