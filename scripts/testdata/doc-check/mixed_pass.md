# Mixed fixture (passes)

Names tokens inside backticks and inside a fenced block; the only
plain prose is non-banned. Must PASS.

The token `robust` is a meta-mention.

```
world-class
industry-leading
```

Plain prose: the gate fails closed on vague marketing language.
