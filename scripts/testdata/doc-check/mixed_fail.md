# Mixed fixture (fails)

Mentions tokens inside backticks (allowed) and in plain prose (banned).
Must FAIL because the plain-prose line below carries the marketing claim.

The token `robust` is a meta-mention and should not trip the gate.

This system is battle-tested and ready.
