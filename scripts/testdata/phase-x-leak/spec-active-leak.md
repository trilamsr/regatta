---
title: Active spec with Phase-X leak
status: active
summary: References tenant_id with no Phase-X opt-in marker; must fail.
---

# Active spec with leak

This spec adds a multi-tenant tenant_id column to a new table. That is
Phase-X scope (W8 + W12) — flagged because the spec is `status: active`
without `phase: x-forward-fit` opt-in.
