---
title: Active spec with htmx leak
status: active
summary: References htmx with no Phase-X opt-in marker; must fail.
---

# Active spec with htmx leak

This spec adds an htmx-progressive admin page in Phase S. That is Phase-X
scope per the self-host brief §1 + §4 ("No htmx UI needed (CLI works for
solo operator)") — flagged because the spec is `status: active` without
`phase: x-forward-fit` opt-in.
