---
title: Active spec with forward-fit opt-in
status: active
phase: x-forward-fit
summary: Active spec that intentionally seams to Phase-X (W8/W11) — opt-in.
---

# Active spec with forward-fit seam

This W6 OTel backbone emits a placeholder `regatta.tenant_id` resource
attribute today, with the W8 RBAC hand-off pinned by the
`phase: x-forward-fit` opt-in. Phase-X tokens are intentional here.
