---
title: Active spec naming Phase-X tokens only in fenced code
status: active
summary: Tokens appear only inside fenced code blocks — author marks them as literal.
---

# Active spec, fenced mention

This active spec mentions Phase-X tokens only inside fenced literal blocks,
matching the doc-check convention that code spans/fences are exempt.

```text
tenant_id, RBAC, Stripe, Sigstore, Rekor, blackboard, Temporal
```

Outside the fence the prose is Phase-S-only.
